# mcm51-c 教训卡片（下题强制）

> **用途**：本题教训沉淀，下一题开工前读。规则正文见 `D:\数学建模\docs\建模赛后诊断.md` §6；本卡 = 本题视角。

## 关联题

- **同届**：mcm51-b（排程优化）——口径防线 v3 起源（假设 9 教训），见 `problems/mcm51-b/` 与 docs/20
- **同型**：mathorcup16-c（数据驱动）——操作面对号实例，可对照本题 S1/S2 产物
- 交叉参照规则：同届看口径、同型看方法族与 L4 参数

## 本题教训（E1–E5 → 通用化）

| # | 教训 | 通用规则 | 下题检查点 |
|---|------|----------|------------|
| 1 | **看见却讲错**：Q5 写出"恢复 25 天"却在结论里写成误报——观察与叙事不一致 | 解释失败专项（七问第 7 条；G4） | 评审时搜"正文描述过、结论否定"的现象 |
| 2 | **敏感性扫到更优口径却留在附录**：smooth_w=72 已进探针带，主口径没改 | 敏感性 → 主模型决策（G1） | 敏感性档位改善结构一致性 → 必须回流主口径并留决策记录 |
| 3 | **Δy 建模不声明初值**：y₀=0 积分 → 量级放大 2 倍 | 状态重建检查（G2） | 累积量模型：y₀ 是什么、重建公式何时成立、非零初值对照 |
| 4 | **统计异常 ≠ 领域异常**：MAD 合法但抓到 1123 个（官方 93），天气/趋势被当故障 | 领域定义先于检测器（G3） | 定阈前先写"本题要抓什么"的操作定义 |
| 5 | **指标与损失口径不一致**：损失降权尖峰，报告的全序列 MAE 仍含尖峰 | 指标与损失一致 | 报告指标与训练目标同口径，或补对照 |

## 做对了的（保留）

- 数据探查先行（data_profile.md + 锚点）→ L1 合格
- 方法替代 vs 方法错误的分辨（K=2 SSE / 增量回归 / MAD 均为合法替代，不是"没用 PELT"）
- 数字 100% 回源、零造数；拒绝虚高 R²（增量回归 vs 水平+AR 的判断正确）
- 口径防线应用（爆破空值=非事件、实验集位移为空、Q3 无时间列声明）

## 一句话

**C 题证明体系已能"看懂数据、立对模型、数字回源"；下一题的主战场在 L2 结构识别（领域模式）与
L5 解释一致性（叙事与观察对齐）——前者靠领域知识卡片，后者靠 G4 检查与人的七问第 7 条。**
