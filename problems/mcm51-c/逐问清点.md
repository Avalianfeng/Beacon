# 2026 五一赛 C 题（边坡预警）· 逐问清点（问题分解表）

> 阶段 3 产物（方向生产协议 brief-playbook §4）：逐小问最终清点，供后续填 brief 与评审对照。**这是清点不是解题**。
> 数据事实一律引用 `data_profile.md`（标 L=行号）；口径引用 `口径风险-第二读者.md`（R/S/D/F 编号）；方法族引用 `exploration.md`（§9/§11）。
> 覆盖范围：Q1、Q2、Q3.1、Q3.2、Q3.3、Q4.1、Q4.2、Q5.1、Q5.2，共 9 小问。

---

## 0. 全题口径总表（全局红线，每问生效，先钉死再写题）

| # | 红线 | 依据 |
|---|------|------|
| G1 | 采样 10min，**10min=1/6h**；所有速度一律 **mm/h** | 题面注 1.1/2.1；口径风险 §3.1；名词解释(6) |
| G2 | 时间基准逐附件不同，**跨附件不可混用**：附1=2024-01-01 **10:00**（文件实际，题面"0 时起"与文件冲突，以文件为准）；附2=2024-05-04 00:00（编号↔时刻）；附3=无时间列，仅编号 1..N；附4 训练=2023-05-01 16:40、实验=2025-05-01 16:40；附5=2024-01-01 16:40 | data_profile L38/L94/L146/L211/L286；口径风险 §3.2 |
| G3 | **时序前向/滚动交叉验证，全题禁止随机切分**（未来泄漏=评阅高频扣分点），Q1 的 CV 亦须前向 | 认知地图 §7🔴/§12 第 1 条 |
| G4 | **爆破两字段空值 = 无爆破事件语义**（Q4/Q5），构造"是否爆破"二值+事件数值特征，**禁止当缺失插补**；与 Q3"空=未填写=缺失"语义不同 | 题面 Q4/Q5 原文；口径风险 S4/R10/D3 |
| G5 | **实验集表面位移 100% 空 = 预测目标**（Q3.3/Q4.2），不是缺失、不是补齐对象 | data_profile L168/L232/L244 |
| G6 | 表 3.1 字母映射固定：a=降雨量 / b=孔隙水压力 / c=微震事件数 / d=深部位移 / e=表面位移；表 3.2 用字母组合 | 题面表 3.1；口径风险 S9 |
| G7 | 附3 训练集列名带 `a:..e:` 前缀、实验集无前缀，预处理第一步统一列名 | data_profile L142–143；口径风险 D1 |
| G8 | 表 4.1 时间→行号换算公式必须给出（t=基准+(行号−1)×10min），防"全对却错位" | 认知地图 §12 第 6 条 |

---

## 1. 问题分解表（9 小问）

| 小问 | 交付物类型 | 出题人想看什么 | 消耗附件 | 候选方法族（≥2，含淘汰理由） | 单位/口径 | 必配图表 |
|---|---|---|---|---|---|---|
| **Q1** | **回归方程与系数**（校正模型：A→B 映射）+ 交叉验证方案 + 偏差量化指标 + **表 1.1 五值** | "对数据 A 进行校正，使校正后的结果与数据 B 的偏差尽可能小；采用交叉验证并给出偏差量化指标"（题面 P19）；5 个验证点 x 是 **A 列位移取值并精确命中唯一行**（data_profile L72–80/F1）→ 模型必须同时处理"恒正偏差+线性漂移"双结构（L70），且对跳变时刻也给出校正值 | 附件1 Sheet1 全量 10000×3（时间/数据A_光纤位移计数据_mm/数据B_振弦式位移计数据_mm，L22–30）；A、B 均 0 缺失（L51）；A−B 漂移 +0.0268 mm/h、r(A,B)=0.9996（L69–71）；表 1.1 x=7.132/18.526/84.337/123.554/167.667 → 行 1325/8790/2255/4672/8205，对应 B=5.729/15.801/73.838/108.395/147.348（L74–80），**其中 4 个是 A/B 同步跳变下跳点**（L82） | ①鲁棒线性+时变漂移（B≈β0+β1·A+γ·t，Huber/LTS 抗 A 独有 98 处 ±20–30mm 尖峰，L55–56）**主**；②常数平移 y=A+mean(B−A) 基线——无法处理全期 ≈43mm 漂移（首 24h 1.57→末 24h 45.35，L70），**淘汰为主**；③局部加权/样条对照——过拟合尖峰、外推性差（认知地图 §9，R1） | 单位 **mm**（列名自带）；时间基准冲突（题面"0 时起" vs 文件首行 10:00，L38）→ **裁决：以文件实际为准并在文中声明**，备选按题面重排（仅叙事，无数值影响）；CV 用前向/滚动（G3）；偏差指标 MAE/RMSE/R² 至少两个；跳变验证点**不剔除**直接对 B 读数比对并报峰值偏差，备选口径：剔除跳变点重评（差异：峰值偏差项变化大、均方误差影响小） | 校正前后时序对比（A 校正 vs B 叠加）、残差时序/QQ 图、前向 CV 误差曲线、**表 1.1 填写** |
| **Q2** | **变点位置+准则**（两个转换节点 + 区分"噪声/瞬时跳变 vs 真实转换"的**核心准则**）+ 分段模型（参数+检验）+ 各阶段平均速度 | "转换节点"→**变点检测**；"给出区分…的核心准则"→**题面点名必答硬项**（P33，见 §2.1 第 1 条）；"对各阶段分别建立数学模型，给出模型参数并进行检验"→分段回归+显著性检验；"计算各阶段表面位移平均速度"→注 2.2 口径（P37） | 附件2 Sheet1 10000×2（编号/表面位移_mm，L90–91）；隐含时间 2024-05-04 00:00 起 10min（L94–96）；0 缺失（L104）；噪声级 ±4 mm/10min、速度 3σ≈1.85（L121）；三段粗划转折 ~编号 7900–8000（06-29）/9500–9600（07-09）（L133）；**负差分占 31.99%、日 12 回退**（L100–101） | ①分段线性回归+结构变点（PELT/BinSeg，ruptures 类）+ 速度显著性检验（Chow/似然比）**主**；准则=**持续性+幅度+模型差异三重判据**（认知地图 §9）；②纯速度阈值基线——单步噪声 ±4 超 3σ 无法区分瞬时跳变与真实加速、且无"准则"输出（R4），**淘汰为主**；③贝叶斯变点对照——节点带不确定性但节点数先验敏感、计算重、准则叙事绕 | 位移 **mm**、速度 **mm/h**（10min=1/6h）；注 2.2 阶段速度=阶段位移变化总量/阶段总持续时间（P37）；编号↔时刻公式 t=2024-05-04 00:00+(编号−1)×10min 必须给；**回退口径（新增裁决）**：阶段速度取"阶段末−初"（净变化）**主**，备选 Σ\|Δ\| 总路径长（差异：负回退幅度小，影响 ≤0.1 mm/h 量级，须声明口径）；负斜率回退段不构成新阶段 | 位移-编号时序+分段拟合+变点垂直标注、各段残差图、速度序列+阈值/判据示意、阶段均速柱状图 |
| **Q3.1** | **去噪+插补管线**（逐步骤数学依据） | "高效去噪和缺失值补齐，并详细阐述各步骤的数学依据"（P47）→ 管线化+每步公式原理，不是只交结果 | 附件3 训练集 sheet 10000×6（编号+a:降雨量_mm+b:孔隙水压力_kPa+c:微震事件数+d:深部位移_mm+e:表面位移_mm，L142–143）；各列缺失 4.6–5.6% **零散**（最长连续段 ≤3 行，L174）；降雨右偏 43% 零（L154）、微震计数 0–9 且 27.7% 零（L156）→ 不能当正态连续量 | ①**按变量类型分治主**：连续量（孔压/深部位移/表面位移）小波/中值保边去噪+零散短段线性/样条插值；计数（微震）不去噪保整数、缺失近邻/众数；降雨重尾用局部中值防均值污染；每步给数学依据（Donoho-Johnstone 阈值等）；②滑动平均基线——不保边、边界损失、抹掉跳变（与题面数据含跳变冲突），**淘汰为主**（R6）；③MICE/卡尔曼对照——对成段缺失弱、状态噪声参数难定、"逐步骤数学依据"叙事绕（R7） | mm / kPa / 计数；"空=未填写=缺失"**仅限本问训练集**（L5/L176）；实验集表面位移 100% 空=**预测目标**，不是本问补齐对象（L168）；**去噪保边是隐含要求**（与 Q3.2 异常检测、Q2 准则呼应，不得抹掉真实跳变） | 去噪前后对比（含跳变/缺失窗口放大）、缺失位置分布图、插补前后统计对比（均值/std/缺失段） |
| **Q3.2** | **异常点清单表**（表 3.1 单变量数量 + 表 3.2 共同异常点清单） | "同一时间点 ≥2 个监测变量同时异常"→**多变量联合异常**（S8）；表 3.1 填 a–e 数量+总数、表 3.2 填编号+异常变量字母组合（样例"abc"，S9） | 附件3 训练集**预处理后**（Q3.1 输出）；编号 1..10000（无时间列） | ①单变量稳健判据（MAD/IQR×k，计数/重尾变量单独定阈）→ 同刻 ≥2 变量聚合，**阈值 k 敏感性扫描** **主**；②3σ 判据对照——对异常污染敏感（均值/std 被拉大→漏判；降雨 3σ 外 2.9% vs 孔压 0.2% 极不均衡，L171），异常点数量可能差几十%（R5），**淘汰为主**；③PCA 重建残差/Mahalanobis 多变量联合对照——对稀疏计数（微震 0–9、降雨 43% 零）不适用，且与题面"先单变量再聚合"两表结构错位 | 计数；字母映射固定 a/b/c/d/e（G6）；表 3.2"时间点对应编号"用**训练集编号**（无时间列）；**R5 裁决：MAD/IQR 为主判据**，双跑 3σ 作稳健性证据；备选 3σ（差异：异常点数量可能差几十%，直接改表 3.1 数字） | 各变量时序+异常点红点标注（去噪前后对比）、共同异常点时间分布（叠降雨/微震事件）、**表 3.1/3.2 填写** |
| **Q3.3** | **关联系数与贡献度**（定量排序）+ 实验集估计表面位移散点图 | "定量分析边坡表面位移与…之间的关联，评估各因素对表面位移变化的贡献程度"（P75）→ 回归+贡献度；"将模型应用于实验集，估计对应的表面位移并绘制散点图"（P75） | 附件3 训练集（预处理后）建模；实验集 5000×6（**表面位移 100% 空=预测目标**，L168）；相关：深部位移-表面位移 0.898、孔压-深部 0.899、孔压-表面 0.833、降雨-表面 0.598、微震-表面 0.131（L190–194） | ①**滞后对齐（CCF 定降雨→孔压→位移滞后，小时~天级）+ 多元回归标准化系数/增量 R²（方差分解）主**，物理符号检查（孔压↑→位移↑ 为正合理）；②SHAP/随机森林重要性对照——**共线下重要性分裂不稳定**（深部-表面 0.898），只作佐证（R8/R15）；③逐步回归对照——共线下选择不稳定；**共线处理：偏相关/增量 R²/SHAP 三方对照**（认知地图 §5） | 表面/深部位移 mm、孔压 kPa、降雨 mm、微震计数；**滞后以 10min 步数表达**（附3 无时间列，L146）；实验集预测=估计值非真值；散点图横轴未明（认知地图 §4⚪）→ **裁决：横轴=编号、纵轴=估计位移**，文中标注无法对照真值；备选：另画训练集拟合 vs 实际散点作模型验证 | CCF 滞后图、贡献度三方对比条形图、训练集拟合残差图、实验集估计位移散点图 |
| **Q4.1** | **分阶段预测模型**（结构+参数+评估） | "不同阶段中降雨、爆破、微震等外界扰动对位移的影响可能发生显著变化"（P85）→**分阶段建模**（阶段分组或阶段×特征交互）；"建立分阶段数学模型，分析并预测表面位移变化规律"（P85） | 附件4 训练集 sheet 10000×7（时间/表面位移_mm/降雨量_mm/孔隙水压力_kPa/微震事件数/爆破点距离_m/单段最大药量_kg，L206–208）；2023-05-01 16:40→07-10 03:10（L211）；**训练集无阶段标签需自识别**（D2/S3）；爆破 41 非空/99.59% 空（L224–225）；降雨 97.97% 零、表面位移-降雨 r≈0（L221/L266）；表面位移-微震 0.614（L266） | ①**增量口径（对位移增量/速度建模，阶段分组或阶段×特征交互；爆破="是否爆破"二值+事件数值特征；GBDT 处理稀疏）主**+前向验证（认知地图 §9）；②水平+AR 口径对照——易"复制上一步"虚高 R²，与增量口径互斥（认知地图 §3/§9/§11），**二选一为主**；③LSTM 对照——可解释性弱、跨年泛化差，一段结论即可 | 位移/降雨 mm、孔压 kPa、微震计数、爆破距离 **m**、药量 **kg**（D3 确认）；爆破空值=无事件（G4/R10），二值编码不插补；训练集阶段边界用 Q2 方法自识别（与实验集节点不一致，S3）；**降雨 97.97% 零→构造滞后累计特征（API/前期雨量）而非瞬时值（新增裁决，见 §2.2 第 4 条）** | 训练集分阶段拟合+阶段分界标注、增量 vs 水平口径误差对比、前向验证预测-时间图、残差图 |
| **Q4.2** | **分阶段预测模型+预测值表**（表 4.1 五时间点） | "实验集与训练集在**对应阶段**的表面位移变化规律一致"（S2 分级①原文）→ 按**对应阶段**对齐，非时间对齐；"阶段转换节点已在实验集的阶段标签中给出（**训练集与实验集的阶段转换节点不一致**）"（S3）→ 实验集用给定标签、每段套用训练集对应阶段模型；"绘制表面位移随时间变化的图像…补全表 4.1"（P89） | 附件4 实验集 sheet 5000×8（时间/阶段标签/表面位移100%空=目标/降雨/孔压/微震/爆破距离/药量，L207–208）；2025-05-01 16:40→06-05 09:50（L211）；阶段 1/2/3=2960/1530/510 行，边界 2025-05-22 05:50、06-01 20:50（L256–257）；爆破 24 非空（L236–237） | ①**阶段标签分组+各阶段套用 Q4.1 对应模型（增量口径逐点累加恢复位移水平）主**；②全局单模型不分阶段对照——违反 S2/S3 分阶段语义、阶段影响被平均化，**淘汰为主**；③LSTM/seq2seq 对照——误差累积+跨年泛化弱 | 同 4.1；**跨年泛化（2023 训练→2025 实验）须论证并报预测区间**（认知地图 §6🟡，F4）；时间→行号公式 t=2025-05-01 16:40+(行号−1)×10min 必须给；**表 4.1 五点换算（以公式为准，实现时逐点校验）**：2025-05-09 12:00→行 1125（阶段1）、05-27 08:00→行 3693（阶段2）、06-01 12:00→行 4437（阶段2）、06-03 22:00→行 4785（阶段3）、06-04 01:40→行 4807（阶段3）→ **按点所在阶段套对应模型，不是同一模型全填** | 实验集预测位移-时间曲线（阶段分界标注）、**表 4.1 填写**、预测区间带（可选）、训练/实验阶段模型参数对照 |
| **Q5.1** | **变量组合+误差对比**（最优组合模型+原理） | "选取 5 类变量构建数学模型…评估不同变量组合下的表面位移数据误差，给出最优的变量组合模型，并解释原理"（P105）→ **6 选 5 穷举 6 组**（S10）+误差对比+最优组合解释 | 附件5 Sheet1 9450×8（时间/表面位移/降雨/孔压/微震/干湿入渗系数/爆破距离/药量，L282）；2024-01-01 16:40→03-07 07:30（L286）；位移 0→3557.8 mm（L292）；干湿入渗 0.369–0.844（L296）；爆破 47 非空/99.50% 空（L297–298）；相关：表面位移-微震 0.364、-药量 0.317、微震-干湿入渗 0.374、孔压-干湿入渗 −0.293（L320–324） | ①沿用 Q4 增量口径同族模型，**C(6,5)=6 组穷举+前向 CV 比 RMSE/MAE 主**，最优组合+原理（含**干湿入渗×降雨交互项**检查，认知地图 §3/§9）；②Lasso 自动选变量对照——**不出组合表、与题面格式要求不兼容**（R12，认知地图 §11），**淘汰为主**；③全 6 变量模型作上限参照 | mm / kPa / 计数 / **无量纲**（干湿入渗）/ m / kg；爆破空值=无事件（G4）；误差指标 RMSE/MAE（mm）必报，**附相对误差**（位移量级 0–3558mm，绝对值意义有限）；降雨 98.24% 零（L293）→ 同 4.1 需滞后累计特征 | **6 组合误差对比条形图**、最优组合拟合 vs 实际散点图、残差图、（可选）交互项效应图 |
| **Q5.2** | **速度阈值预警机制**（分级阈值+合理性） | "以表面位移速度为预警指标…分阶段分析数据变化规律，并据此构建滑坡预警机制（如表面位移速度达到什么阈值时发出预警等），并解释其合理性"（P109）→ **分阶段速度阈值+分级机制+合理性**；首句"边坡介质差异会导致边坡阶段划分与预警阈值不同"（P109）→ 介质差异语义须点名（见 §2.2 第 3 条） | 附件5（5.1 最优模型的速度输出）+ 阶段划分（Q2 方法在附5 上识别）；附5 单步最大 Δ=+32.62 mm/10min（L313） | ①**分阶段速度阈值=各阶段速度分布统计（均值+kσ/高分位 P90–P99）+ 误报-漏报权衡曲线选操作点 + 速度倒数/Saito 切线外推给剩余时间 主**（认知地图 §7/§8）；②固定领域标准阈值对照——数值需一手文献核实、与本数据量级可能不匹配，只作合理性佐证非主源（R13）；③纯物理判据（Saito/Voight）对照——数值需一手文献（认知地图 §10） | 速度一律 **mm/h**（10min=1/6h）；预警分级 2–3 级（注意/预警/报警），阈值给确定方法（分位数/权衡曲线）非拍脑袋；**误报/漏报不对称**（漏报=生命损失、误报=疏散成本）显式权衡（认知地图 §8）；**R13 裁决：主=数据驱动分位数**，备选=固定标准阈值（差异：阈值数值不同、预警时点差小时~天级，须声明来源口径，R13 分级③经验假设） | 分阶段速度曲线+阈值水平线、**误报率-漏报率权衡曲线（操作点标注）**、（可选）速度倒数/剩余时间曲线 |

---

## 2. 「我们有没有可能完全没看到某个重要因素？」

对照认知地图 v1 + 数据画像 + 口径风险，逐条裁定。**已覆盖=前四份输入已点名且裁决明确；未覆盖=输入未点名/未完全点名，须补进 brief；为什么不需要=明确排除项。**

### 2.1 题面/口径风险点名项（特别检查，7 条）

1. **Q2"核心准则"是否被当作必答硬项？→ 已覆盖（且必须列为 Q2 主交付物）。**
   题面 P33 原文"给出区分'噪声/工程扰动引起的瞬时跳变'与'真实的阶段转换节点'的核心准则"是**命令句**，不是建议。认知地图 §9 主方法（持续性+幅度+模型差异三重判据）、§12 必须体现第 2 条均已点名。若论文只给节点位置不给准则，Q2 丢一半分。**埋点位置：Q2 交付物类型列已写"变点位置+准则"。**

2. **时序泄漏（随机切分）是否全题禁止？→ 已覆盖（全题红线 G3）。**
   认知地图 §7🔴、§12 第 1 条。补充强调：**Q1 的 CV 也必须前向**——A/B 存在 +0.0268 mm/h 线性漂移（L70），随机 K 折会把漂移结构泄进训练折；Q3.3 训练/实验两 sheet 独立，但模型内部验证、Q4/Q5 的前向验证一律滚动切分。**埋点位置：G3。**

3. **降雨滞后（小时~天级）是否每处关联/预测都考虑？→ 已覆盖（机制上），但三处附件语义不同，需分别处理。**
   认知地图 §3🔴、口径风险 R9 点名。**关键事实**：附3 降雨量-表面位移 r=0.60（中强）、43% 零（L154/L190）；附4/5 降雨量 ≈98% 零、与表面位移 r≈0（L221/L266/L320）——**跨附件降雨量语义不同，不可直接类比**（L334，F2）。落地规则：Q3.3 用 CCF 定滞后（以编号步数表达，附3 无时间列）；Q4/Q5 因降雨极稀疏须构造**滞后累计特征（API/前期雨量指数）**而非瞬时值。**埋点位置：Q3.3 单位/口径、Q4.1 单位/口径、§2.2 第 4 条。**

4. **爆破空值事件语义、实验集表面位移=预测目标（两个最易翻车的数据语义）→ 已钉死。**
   口径风险 S4/R10/D3/F3、data_profile L339 均已点名。补充三个衍生事实必须一并钉死：①**微震≠爆破标识**（附4 训练集 41 个爆破行中 22 行微震=0；微震>0 的 5251 行绝大多数无爆破，L253/F3）→ 爆破特征只能由"爆破两字段非空"构造；②**爆破与降雨互斥**（爆破行降雨全 0，L252）→ 降雨×爆破交互项无样本、不可估计，勿构造；③附3 实验集表面位移 100% 空是预测目标，**与本问（Q3.1）的缺失补齐完全两回事**（G5）。**埋点位置：G4/G5。**

5. **Q4 训练 2023 vs 实验 2025 跨年泛化、训练集无阶段标签需自识别 → 已覆盖。**
   口径风险 S3/D2/F4 点名：训练集无阶段标签（用 Q2 方法自识别）、实验集标签只用于分组评估**不可当训练特征**（防泄漏）、跨年泛化必须论证并报预测区间（认知地图 §6🟡）。**埋点位置：Q4.1/Q4.2 口径列。**

6. **贡献度评估的共线性（深部位移与表面位移 0.90 相关）→ 已覆盖。**
   附3 深部-表面 0.898、孔压-深部 0.899、孔压-表面 0.833（L190–194）；认知地图 §5🔴、口径风险 R15。Q3.3 用偏相关/增量 R²/SHAP 三方对照；Q4 爆破两列高度联动合并处理。**埋点位置：Q3.3 候选方法族。**

7. **预警的误报/漏报不对称、剩余时间估计 → 已覆盖。**
   认知地图 §8🟡：漏报=生命损失、误报=疏散成本+信任损耗，阈值须显式权衡；预警时效性给预计剩余时间（速度倒数/Saito 切线外推），10min 采样下快速段窗口短。**埋点位置：Q5.2 必配图表（权衡曲线）+ 候选方法族。**

### 2.2 独立复核发现的**未点名/未完全点名**因素（前四份输入没有，必须补进 brief）

1. **附件1 题面"从 2024 年 1 月 1 日 0 时起" vs 文件实际首行 2024-01-01 10:00（L38）——未点名。**
   口径风险 F1–F5 未覆盖此冲突。**裁决倾向**：以文件实际为准（首行 A=B=0 疑似初始归零参考点），论文中显式声明"题面注与文件不一致，按文件实际时间轴"。备选口径：按题面把首行视为 0 时（时间仅作漂移项自变量，线性漂移对平移不敏感，数值无差异）。差异量级：无实质影响，但**不声明会被评阅认为是读数错误**。

2. **附件2 负速度/回退现象与注 2.2"位移变化总量"的口径——未点名。**
   附2 负差分占 31.99%、日 12 平均速度为负（20.0→19.4 mm，L100–101）。注 2.2 说"该阶段表面位移变化总量/该阶段总持续时间"。**裁决倾向**：阶段速度=阶段末位移−阶段初位移（净变化）为主口径；备选口径：Σ|Δ| 总路径长（差异：回退幅度小，阶段均速差异 ≤0.1 mm/h 量级，但必须声明）。衍生要求：变点检测对负斜率回退段须鲁棒（回退不构成新阶段，避免误切分）。

3. **Q5.2 首句"边坡介质差异会导致边坡阶段划分与预警阈值不同"的介质差异语义——未点名。**
   题内只有一段边坡数据，"介质差异"无法在数据内直接对照。**裁决倾向**：主线按后半句明确指令执行——"分阶段分析数据变化规律，据此构建预警机制"（同坡内分阶段阈值，与 Q2 阶段轴一致）；"介质差异"作为合理性讨论收尾（干湿入渗系数 0.369–0.844 是附5 独有的介质/入渗能力代理，可论证其作为阈值调节变量的物理机制）。备选口径：按干湿入渗系数分档定阈值（数据为平滑连续值、无离散档位，**证据不支持**）。差异量级：阈值数值随口径变化，预警时点差小时级。

4. **附4 训练集降雨 97.97% 为零（L221）→ 瞬时降雨特征几乎无信息（表面位移-降雨 r≈0，L266）——未完全点名。**
   认知地图 §10 有"前期雨量指数 API"文献线索，但 Q4 主方法族未点名落地。若分阶段模型直接用瞬时降雨列，该特征形同虚设。**必须**构造滞后累计特征（过去 N 小时/天累计雨量、API）再入模型，并报告特征构造对误差的影响。附5 同理（降雨 98.24% 零，L293）。

5. **附4 实验集快速段仅 510 行 ≈3.5 天（L256）——未点名。**
   快速段样本少+跨年外推 → Q4.2 预测区间在快速段显著变宽，须报区间并说明不确定性来源（数据量+跨年泛化），不要只给点预测。

6. **附3 训练集与实验集是否同一时段/同一边坡未知（无时间列，L146）——未点名。**
   Q3.3 把训练模型套到实验集隐含"分布相似"假设。若两集跨时段（类似 Q4 的 2023 vs 2025），预测即外推。**要求**：文中显式声明该假设与不确定性，给预测区间。

7. **表 4.1 五个时间点分别落在阶段 1/2/2/3/3（行 1125/3693/4437/4785/4807）——未点名。**
   认知地图 §12 第 6 条只要求给换算公式，未指出五点跨阶段。预测时必须**按点所在阶段套用对应阶段模型**，不是同一个模型填满全表（否则与 S2"对应阶段规律一致"矛盾）。**埋点位置：Q4.2 口径列（已写入）。**

8. **附5 爆破行微震 41/47>0（L310）vs 附4 训练集爆破行 22/41 微震=0（L253）——未完全点名。**
   F3 已说"微震≠爆破标识"，但未点名**跨附件爆破-微震关系不一致**：附5 爆破行微震基本>0（可能爆破诱发微震），附4 却多为 0。跨附件不可类比，各问独立建模（L336 同旨）。**意义**：附5 中"是否爆破"与微震存在部分共现，Q5.1 组合比较时注意两特征的信息重叠，可在原理解释中说明。

9. **附3 训练/实验列名前缀不一致（a:..e: vs 无前缀）——D1 已点名，但须在 brief 实现注记钉死**（预处理第一步统一列名，防建模时列对齐错位）。

### 2.3 明确排除项（为什么不需要）

- **卡尔曼/状态空间、LSTM 深挖**：篇幅大、可解释性弱、跨年泛化差（认知地图 §12）→ 只留一段对照结论，不做主方向。
- **MICE 多重插补细节**：与"逐步骤数学依据"叙事重叠；本数据缺失零散短段（最长 ≤3 行，L174），线性/样条插值已够 → 点到为止。
- **贝叶斯变点精细后验**：节点数先验敏感、计算重、准则表达弱（R4）→ Q2 用 PELT/分段线性主+Chow 检验即可。
- **纯物理阈值作主源（Saito/Voight 数值）**：数值需一手文献核实、与数据驱动口径不同源（R13 分级③）→ 只作合理性佐证。
- **微震前兆独立章节、空天地背景综述**：题面只要求贡献度/预警，背景一句带过（认知地图 §12）。
- **附件3 深部位移"内部先行指标"独立亮点**：可作可省（认知地图 §8⚪），非必做。
- **附3 实验集散点图横轴争论**：横轴未明是题面空缺，裁决=编号横轴+无法对照真值声明（认知地图 §4⚪），不展开成风险。

### 2.4 结论

- **已覆盖 7/7 题面点名检查项**：核心准则、时序泄漏、降雨滞后、爆破空值/预测目标语义、跨年泛化与无标签、共线性、误报漏报不对称+剩余时间。
- **新增 9 条未点名/未完全点名项**（§2.2），其中 **3 条必须写入 brief 口径防线**：附1 时间基准冲突（§2.2-1）、附2 回退口径（§2.2-2）、Q5.2 介质差异语义（§2.2-3）；其余 6 条写入实现注记（§2.2-4~9）。
- **无"整问漏看"级别缺口**；最大残余风险是 Q5.2 介质差异语义的解读（§2.2-3）与附4 降雨极稀疏下的特征构造（§2.2-4），二者已在表中给出裁决倾向+备选口径+差异量级，评审对撞时可据此再裁决。

---

*逐问清点结束。供填 brief 与评审对照使用；方法细节以认知地图 §9/§11 与口径风险 R 表为并集。*
