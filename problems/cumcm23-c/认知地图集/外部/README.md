# 认知地图集 / 外部

对撞「外部侧」全部落在这里（学-17 后收拢）。**产本地地图时禁读本树中的地图 / 轮次 / 外发**；编号切片在 `索取/`，本地若重跑可读。

```text
外部/
  地图/          第二家认知地图定稿
  轮次/          第二家原文（本轮未落盘）
  外发归档/      打包 / 交棒 / 回填说明
  索取/          编号切片 01–15 + compute（本机算给外部的证据）
```

| 子路径 | 内容 |
|---|---|
| `地图/认知地图-GPT-9-4-17_02.md` | 0.5-闭环外部地图 |
| `轮次/` | 无；当时只存了地图与打包，未存对话框原文 |
| `外发归档/` | 轮1/轮2 打包 md+png、交棒、回填说明 |
| `索取/` | 01–15 csv/png、`_compute.py`、`_compute_r2.py` |

题根旧 `索取/`、`对撞外部/`：**已废止**（若 IDE 仍锁着空 `索取/` 目录，关掉占用后手动删）。

打包命令示例：

```text
python scripts/pack_external_upload.py --dir problems/cumcm23-c/认知地图集/外部/索取 --stem 给第二家-打包
```

（过程件打完再挪进 `外发归档/`。）
