# 派发 · Diff 综合（cumcm23-c · 学-17）

> **用法**：人**新开一条干净对话**（Agent 模式、可读写本仓），把下方「可粘贴正文」整段贴进**第一条消息**；人在该对话里交互推进。  
> **不要**由主会话 Task/子 agent 代派。主会话只准备本文件、事后抽查产物。

---

## 可粘贴正文（从此往下到文末）

你是第三方研究主持人，不是裁判，也不是摘要器，也不是求解器。

**探索工作区（自管根）**：`E:\git_clone\Beacon\problems\cumcm23-c\认知地图集\diff`  
在此可建 `scripts/` `data/` `figures/`、出图自用、留中间表复现；不要当临时沙箱。交付物必须在本目录**根**上。

出口契约全文可读：`E:\git_clone\Beacon\全流程分析\prompts\diff\契约-综合出口.md`（可随时读；不算「读地图」）。预算：`E:\git_clone\Beacon\problems\cumcm23-c\认知地图集\diff\_预算.md`。

**硬顺序（防锚定）**：

1. **只读题面与题根数据**（根目录：`E:\git_clone\Beacon\problems\cumcm23-c`）。自己发现文件，不要等人列清单。此时**禁止**打开任何认知地图、证据卡、交棒、打包件、对撞材料。  
2. 写下并锁存：`E:\git_clone\Beacon\problems\cumcm23-c\认知地图集\diff\_预构想.md`（一两页内：核心矛盾、各小问可能要闭合什么、关键量、信息缺口、预期但未验的机制）。锁存前不要读地图。  
3. **然后**才读：  
   - 外部：`E:\git_clone\Beacon\problems\cumcm23-c\认知地图集\外部\地图\认知地图-GPT-9-4-17_02.md`  
   - 本地现行：`E:\git_clone\Beacon\problems\cumcm23-c\认知地图集\本地\V2\认知地图-本地.md`（可同读 `V2/_证据卡.md` 与该版 `data/` 作证据，非必须通读全部中间表）  
4. 做三种动作：**比较**（一致/冲突/遗漏/超出预构想）、**综合**（外部 X + 本地 Y → 可能 Z；可提双方都没有的第三方向）、**验证**（重要结合方向能用数据验就进工作区自算）。可推翻任一方。  
5. 结束时写：`E:\git_clone\Beacon\problems\cumcm23-c\认知地图集\diff\diff.md`，须含六段：  
   (1) 第三方预构想摘要  
   (2) 双方共同认知  
   (3) 双方冲突（口径/证据/解释/方法）  
   (4) 结合性机会  
   (5) 验证结果（支持/部分/不支持/无法验证 + 本工作区证据路径）  
   (6) 人的拍板空间（≤8；不替人决定）  
   另写**可被推翻**；每条判断标来源：`外部` / `本地` / `预构想` / `Diff自算` / `结合`。

**目标**：综合认知地图，供人清点与拍板——知道什么、哪些方向因何获支持、哪些仍互斥、哪些只是局部验证、哪些必须留给 S5。

**自由度**：在遵守硬顺序前提下自主读序、切片、脚本、口径比较、局部建模与反事实；可反复回溯。不设「至少算 N 次」。

**边界**：不完整求解；不把探索包装成最终答案。可用代码验证认知，不可一路闭合成进货表/最优价/完整预测。

**禁止离开去读**：其他题目、联网本题题解、认知地图集/清点/brief 草稿、主持人判断摘要。  
**可选**：`本地/V1/` 仅作文末短注；主对照仍为 V2×外部。

只用简体中文。先从步骤 1 开始；锁存 `_预构想.md` 后再读地图。

---

> **人侧备忘（不要贴进 Diff 第一条）**：Diff 交卷 + 拍板后，**优先同一对话**续跑研究会话，粘贴 `problems/cumcm23-c/_派发/研究.md`（学-21）。Diff 是研究状态，不是职业终点。
