# 00-新题上线-演练剧本与执行日志（第一轮：2026-51MCM-Problem B）

- 建立日期：2026-08-21
- 定位：**[00-新题上线流程设计](00-新题上线流程设计.md) 的执行伴侣**——把「新题从 0 到 run」最小闭环拆成**可直接操作的步骤**；以真实题目为脚手架，走一步记一步，暴露的缺口当场登记并回填设计/批次计划。**执行者 = 用户（手动操作）+ 命令辅助**；本文件只定义步骤与检查点，不代执行。
- 与 00 的关系：00 回答「体系应该长什么样」（静态设计）；本文件回答「今天走哪一步、走完得到什么」（动态剧本+日志）；批次计划（02/04/09 等）回答「暴露的缺口怎么修」。
- 防污染条款：**不获取、不读取 2026-51MCM-Problem B 的官方评分标准/参考答案**（A 题教训：MCM-51 已污染开发集）。已核实 B 题目录无评分文件。本演练若跑出 run 产物，登记为**干净题首例**（仍不构成泛化证据，盲测集门槛见 16 阶段 1 / 17#6）。

---

## 一、第一演练题快照

| 项 | 内容 |
|---|---|
| 题号（建议） | `mcm51-b`（与 `mcm51-a` 同竞赛不同题；B04 规范化匹配预期不误报） |
| 标题 | 2026年第二十三届五一数学建模竞赛 B题 多工序协同作业问题 |
| 题面源 | `C:\Users\m1995\Desktop\数学建模-8-16\26年五一赛题\2026-51MCM-Problems\2026-51MCM-Problem B\题目信息.md`（人工整理形态已就绪） |
| 附件 | `B-附件.xlsx`（三子表：工序流程表 / 班组配置表 / 车间距离表） |
| 题型 | 调度/排程优化（作业车间调度变体：多车间 + 双设备工序 + 跨车间运输 + 预算购置） |
| 问题 | Q1 班组1 独立完成 A 车间最短时长；Q2 仅班组1 完成 A–E 五车间；Q3 班组1+2 完成五车间；Q4 500000 元预算购置+调度联合优化 |
| 能力可达性 | **可达**（纯数据驱动，无真实调研/仿真平台/实物实验依赖）→ feasibility.blockers 应为空，可完整闭环 |
| 交付格式 | 结果需填入题面表1～表5（排程表：设备编号/起始时间/结束时间/持续工时/工序编号/班组）——论文表格需与题面表格对齐 |

## 二、本轮演练目标与验收线

- **主目标**：走通「素材盘点 → O1 资产落盘 → spec 确认 → O2 brief 生产 → O3 dry-run 预检」全链路（**0 烧钱**）。
- **v2 交付物重心调整（2026-08-21）**：本轮最重要的产出不是 import 那几个 CLI 命令（手工等价操作完全够用，
  缺口 G1 不阻塞），而是**按 [brief-playbook](..\brief-playbook.md) 完整走一遍后产出的 mcm51-b 干净 brief**
  ——多模型独立提案 + 对撞裁决 + 逐问 checklist，落 `problems/mcm51-b/brief.json`（**无参考值泄漏**）。
  这是全项目当前信息价值最高的实验：它回答「一份干净 brief（人 + 强模型、无答案泄漏）能把分数带到哪里」
  ——目前零证据（brief-v1 的高分是参考值注入撑的，H02）。
- **第二阶段（可选，另起）**：pilot 预跑（半烧钱）→ 全跑 → 评估简报 → 外部评估回填。
- 验收：`problems/mcm51-b/` 下 source 哈希 + problem.md + problem.json（spec v2）齐全；**brief.json 按 playbook
  产出且 `brief check` [OK]、防泄漏条款复核通过**；dry-run 全项通过（或按缺口表登记未实现项）。

## 三、剧本步骤（O1–O4，含决策点）

> 谁做：**U**=用户手动操作；**C**=命令/脚本；**D**=需要用户拍板的决策点。命令未实现时按步骤内「替代动作」手工等价操作，缺口登记到第五节。

### 步骤 1（O1-1）素材盘点与题号确认 【U / D】

- 输入：桌面 `2026-51MCM-Problem B\` 目录（题目信息.md / 题目 docx×2 / 附件 xlsx×2）
- 动作：
  1. 确认以 `题目信息.md` 为题目 md 源（人工整理形态，符合第一段导入约定）；
  2. 确认附件 = `B-附件.xlsx`（另一份 `B-attachment.xlsx` 疑为英文版，登记到 source_files 备查即可）；
  3. **D：确认 problem_id = `mcm51-b`**（目录 `problems/mcm51-b/`，与 A 题并列）。
- 预期产物：素材清单确定
- 检查点：题面/附件/人工整理 md 三者齐备；未引入任何官方评分材料

### 步骤 2（O1-2）资产目录落盘 + source 归档 + 哈希 【U / C】

- 动作：
  1. 建 `problems/mcm51-b/source/`，复制题面与附件（不可变，登记 sha256）；
  2. `problems/mcm51-b/problem.md` = 题目信息.md（事实层）；
  3. 替代动作（若 `problem import` 命令未实现）：手工建目录 + 复制 + 算哈希，登记缺口 G1。
- 预期产物：`source/` 归档 + `problem.md` + 哈希清单
- 检查点：源文件字节不变（哈希可复核）；目录结构与 00 第二节约定一致

### 步骤 3（O1-3）spec v2 起草与确认 【U / D / C】

- 输入：problem.md
- 动作：
  1. 起草 `problems/mcm51-b/problem.json`（spec v2）：title / background / questions（Q1–Q4 各一条）/ source{md_path, source_files+sha256, imported_at, imported_by} / feasibility / data_files / data_dir；
  2. feasibility 初评：checklist 逐项否 → `blockers: []`，assessment 注明「纯数据驱动，可达」；
  3. **D：人工确认 spec 内容**（AI 起草时逐字段核对，解释层可修）。
- 预期产物：`problem.json`（spec v2）
- 检查点：五字段向后兼容（`_read_problem_spec` 可读）；questions 与题面四问一一对应

### 步骤 4（O2）brief 生产（按 brief-playbook）【U / D】

- 动作（v2 重写，2026-08-21；2026-08-21 晚加探索阶段与第二轮消化）：
  1. `brief init --problem problems/mcm51-b/problem.json`（命令未实现则手工建骨架，登记缺口 G1）；
  2. **开放探索（第一轮）**：用 [brief-playbook](..\brief-playbook.md) §2.1 探索提示词（附件「提示词 0」）跑
     「出题人/评阅人/建模者多角度」的题目探索（不急于选方向）——**产出的是思考路径**（可能数千行、
     数十个互不兼容候选点），不是最终认知地图；原始输出归档 `实际操作记录/`；
     **已跑（2026-08-21，GPT）：3364 行思考路径 → [实际操作记录/第一论.md](实际操作记录/第一论.md)**；
  2.5 **探索消化（第二轮追问，同一会话）**：用 playbook §2.5 提示词（附件「提示词 0.5」）把思考流
     收敛为**认知地图 v1**（结构化 ≤150 行、分优先级 🔴🟡⚪、标注互不兼容项、给出 5–8 个论文探索点判定）
     → 人工审阅 → 落 `problems/mcm51-b/exploration.md`；
     工作底稿：[00-演练附件-mcm51-b-checklist草稿](00-演练附件-mcm51-b-checklist草稿.md)
     （2026-08-21：含事实包、逐问分解表草稿、7 个待裁决分歧点、探索/消化/清点/提案四份可直接复制的提示词；先审阅修正再用）；
  3. **多视角对撞**：playbook §3.1 提示词发给 2–3 家不同家族前沿模型（角色分工、**互不看**；允许联网搜索/论文检索；
     每家同样「探索+消化」后再提案，对撞时只交换认知地图 v1）；
     按 §3.3 逐问 diff、分歧裁决——目标是暴露遗漏（A/B/C 之外是否还有看不见的 D），**不是投票定方向**；
  4. **逐问清点**：按 playbook §4.1 逐问过「有没有可能完全没看到某个重要因素」，产出问题分解表；
  5. 按 playbook §5 压缩规则与字段映射写入 `problems/mcm51-b/brief.json`；`brief check` [OK]。
- 候选红线（供 checklist/裁决使用，人工确认后写入）：
  - 结果数值口径：最短时长单位秒、向上取整（假设 5）；起始 00:00:00；
  - 序关系检查：Q2 时长 ≥ Q1（任务超集）、Q3 时长 ≤ Q2（设备超集）——方向级常识锚点，供后置确定性校验；
  - Q4 购置总费用 ≤ 500000 元（硬约束）；
  - 双设备工序必须两类设备都完成工程量方可记完成（假设 2）；
  - 跨车间转运时间不可忽略（假设 4），同车间转移忽略（假设 4 前句）。
- 预期产物：`brief.json`（按 playbook 产出，`brief check` [OK]）
- 检查点：coverage 条目数与问题数对应；brief↔spec 联动（problem_id 匹配不误报）；**无任何官方评分标准/参考值混入**（防泄漏条款，playbook 核心原则 4；探索阶段同样禁止检索评分文件）

### 步骤 5（O3）dry-run 预检 【C】

- 动作：`run --problem ... --brief ... --dry-run`（命令未实现则逐项手工核对，登记缺口）
- 检查项：spec 合法 / brief 合法 / feasibility.blockers 为空（B 题应为空）/ 附件存在且哈希对上 / out 目录无冲突
- 预期产物：预检报告（全项通过）
- 检查点：**任何一项失败 → 先修再进 run**（不烧钱原则）

### 步骤 6（pilot 预跑，第二阶段）【D / C】

- 前置：步骤 5 全通过 + **D：用户确认 pilot 预算**（约为全跑 1/10；跑至 coder 首轮 execute 出数即停）
- 动作：`run --pilot`（命令未实现则手工剧本：跑到 coder 首轮 execute 后人工停止，登记缺口 G6）
- 产物核对（按 [brief-playbook](..\brief-playbook.md) 附录 A 一页简报）：关键数值 / 单位口径（应力力矩、秒分钟等混比）/ 与题面数据对账（表 1～表 5 能否填出）/ 每问方向一句话（brief vs 蓝图实际方向）/ 红线检查
- 检查点：**方向没跑偏、数值口径合理 → 放行全跑；有疑点 → 回源头改 brief 重跑**（原则 8，新 --out，禁止 recover 换 brief）

### 步骤 7（run 全跑，第二阶段）【D / C】

- 前置：pilot 放行 + **D：用户确认预算**（参考 brief-v1 实测 54.1 万 tokens/27.5 min；调度题规模待实测）
- 动作：`supervise --problem problems/mcm51-b/problem.json --brief problems/mcm51-b/brief.json --out runs/mcm51-b-brief-v1 --thread default`（新目录，禁止 recover）
- 产物核对：brief 副本+sha256 ✓ / coverage 全 followed / paper.md 表1～表5 完整且与题面格式对齐 / 最短时长数值出现 / 序关系常识检查通过
- 检查点：degraded 判定原因机器可解释；对照方案缺口按 playbook §六 手工补 + 17#8 模板诚实记录（M4 冻结期）

### 步骤 8（评估与回填，第二阶段）【U / D】

- 动作：evaluation/human_review 后按 [brief-playbook](..\brief-playbook.md) 附录 B 生成评估简报 md → 用户可选发外部网页 AI → 结论回填 brief 源头（原则 8 合法回路）
- 检查点：回填内容标注来源分级；体系内不注入自动投票

## 四、执行日志（操作时逐行填写）

| 日期 | 步骤 | 实际动作 | 结果/产物 | 决策/缺口记录 | 状态 |
|---|---|---|---|---|---|
| 2026-08-21 | 0 | 素材盘点确认 | 题目信息.md + B-附件.xlsx 确认（另有 docx×2/B-attachment 归档备查） | 题号 `mcm51-b` 已确认 | ✅ 完成 |
| 2026-08-21 | 1 | 资产目录 + source 归档 + 哈希 | `problems/mcm51-b/source/` 5 文件 + manifest.json + problem.md | G7（资产就绪） | ✅ 完成 |
| 2026-08-21 | 2 | spec v2 起草 + 确认 | `problems/mcm51-b/problem.json`（title 复合写法防 B04 误报、questions×4、feasibility 空、data_files xlsx 三表摘要） | G4 实测不误报 | ✅ 完成 |
| 2026-08-21 | 3 | brief 生产（playbook：探索→消化→对撞→清点→写入） | ✅ 三轮 GPT 产出（第一论 3364 行思考路径 / 第二轮认知地图 v1 / 第三轮逐问清点+52 条口径裁决）→ 合并映射 `problems/mcm51-b/brief.json`（40 条，`brief check` [OK]，无参考值泄漏，source=human） | 映射依据：playbook §5.1；第三轮原始内容首次保存失败已重存 | ✅ 完成 |
| 2026-08-21 | 4 | dry-run 预检 | ✅ `run --dry-run` 带完整 brief 全项通过（spec/brief/feasibility/附件/out） | G1/G4 已实现并验证 | ✅ 完成 |
| 2026-08-21 | 5 | pilot 预跑 + 简报确认（第二阶段） | 手工监控进行中（G6）：supervise 启动后盯 progress.jsonl 的 coder 首轮 execute，核对 Q1 粗算锚点（41400–41600s）/表1格式/进场200s/方向（异步双设备、移动资源 RCPSP） | 预算已确认（用户：可以开始启动） | 进行中 |
| 2026-08-21 | 6 | run 全跑（第二阶段） | ① 首次启动被 AI 沙箱拦截（LLM 调用 WinError 5）→ 用户手动启动 ✅；② **首次 run 停机**：42 次调用 / 356k tokens 后卡 `model_code_consistency` 一致性 0/10（根因 G8：total_cost 守卫误杀，方向本身正确——代码正确读 xlsx 三表+ffill）→ 守卫已修复；③ **recover 实测空转**（next=() 无节点可续，trace/progress 零新增，B05 语义）→ **结论：只能全新重跑** | 成本：356k tokens 无产物；机制改进建议（人工放行重试 coder）已记入 09 §四 | 待重跑（用户手动，`--force`） |
|  | 7 | 评估简报 + 外部回填（第二阶段） | | playbook 附录 B | 待办 |
|  | 7 | 评估简报 + 外部回填（第二阶段） | | playbook 附录 B | 待办 |

## 五、缺口登记与回填（走一步记一步）

| # | 缺口（预判+实测） | 发现步骤 | 回填目标 | 状态 |
|---|---|---|---|---|
| G1 | `problem import` / `problem show` / `brief init --problem` / `run --dry-run` 命令未实现——**已实现（2026-08-21，O1/O3/O4）**：`problem import/show` + `run --dry-run`（纯机械、不烧 token；tests/test_problem_cli.py + 13 项冒烟全过）；本轮可用真实命令替代手工等价操作 | 步骤 2–5 | 00 第四节（实现前置）+ O1 代码任务 | **已实现（2026-08-21）** |
| G2 | spec v2 无 JSON Schema 文件与校验器（dry-run 预检依赖）——**部分覆盖（2026-08-21）**：`run --dry-run` 已实现可行性/附件/out 冲突检查；独立 JSON Schema 文件仍缺（可选补） | 步骤 2/5 | 00 第三节 + O1 代码任务 | 部分实现 |
| G3 | 评估简报模板：**已由 [brief-playbook](..\brief-playbook.md) 附录 B 覆盖（2026-08-21）** | 步骤 8 | 00 第六节 | 已定义 |
| G4 | B04 规范化匹配对 `mcm51-b` vs 「2026 51MCM Problem B」需实测不误报——**已实测不误报（2026-08-21）**：spec title 采用复合写法（含 ASCII "2026-51MCM-Problem B"），`run --dry-run` 无 WARN | 步骤 4 | 验证 01-批次0 的 0.4 | 已验证 |
| G5 | 题面表1～表5 交付格式 → paper.md 表格对齐 | 步骤 7 | M1 LaTeX 守卫已有；表格模板待观察 | 待观察 |
| G6 | `run --pilot` 命令未实现（v2 新增 O5）；本轮以手工剧本替代（跑至 coder 首轮 execute 后人工停止 + 按 playbook 附录 A 人工核对） | 步骤 6 | [09-pilot模式与早期反证](09-pilot模式与早期反证.md) | 待实现 |
| G7 | mcm51-b 资产已就绪（2026-08-21）：`problems/mcm51-b/` 下 source 归档 5 文件+哈希、problem.md、problem.json（spec v2，data_dir=source，data_files 含 xlsx 三表摘要）、exploration.md 占位、brief.json 空骨架（待对撞产物填入） | 步骤 1–4 | — | 已就绪 |
| G8 | **物流指标守卫误杀通用成本名 total_cost**：非物流题输出校验把 total_cost 当物流指标整份拒绝 → 主方案代码全部被拒 → 一致性 0/10 → 门禁停机（2026-08-21 首次 run 实测，356k tokens 无产物） | 步骤 6 | **已修复（2026-08-21）**：`_GREEN_METRIC_NAMES` 移除 total_cost（通用成本名，成本类题目合法）；物流污染仍由专名（vehicles/service_rate 等）判定；测试已更新+新增 | 已解决 |
|  | （实测新增缺口在此追加） | | | |

## 更新记录

- 2026-08-21 建立：第一轮演练剧本（2026-51MCM-Problem B；用户手动导入，本文件只定义步骤与检查点）。
- 2026-08-21 v2 修订：交付物重心改为「按 brief-playbook 产出的干净 brief」；步骤 4 重写为 playbook 流程；
  新增步骤 6 pilot（步骤 7/8 顺延）；G3 由 playbook 附录 B 覆盖；新增 G6（pilot CLI 缺口，手工剧本替代）。
- 2026-08-21 代码落地第一批：G1 已实现（problem import/show + run --dry-run，纯机械不烧 token）；
  mcm51-b 资产就绪（G7：source 归档+哈希、problem.json spec v2、brief.json 空骨架、exploration.md 占位）；
  执行日志第 0–2 步 ✅；下一步 = 用户按 playbook 产出 brief 填入 brief.json。
