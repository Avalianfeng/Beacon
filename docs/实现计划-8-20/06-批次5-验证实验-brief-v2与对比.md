# 批次 5：验证实验 — brief-v2 与对比

- 状态：**降级为可选实验（2026-08-21）**——A 题 brief-v2 的 brief 含官方参考值（目标泄漏，H02），
  其分数是事后上界；**主验证改为 mcm51-b 干净题真跑**（[00-新题上线-演练剧本与执行日志](00-新题上线-演练剧本与执行日志.md)
  第二阶段）。本文件保留为「若要核对 M3/M2 机制闭环判据（用已知标准比对）时的可选实验协议」；
  发起前仍须用户确认（烧 token）。
- 目标：用一次「机制修复后」的干净真跑，回答 12 §七 的闭环判据；再用对照实验回答「能力来自注入还是机制」。
- 依据：12 §四/§七/§八、16 阶段 0 验收、17 #1/#2/#4/#5。

---

## 一、5.1 brief-v2 真跑（核心实验）

- 前置：批次 0（M1/M5/M6 + 成本遥测 v0）+ 批次 1（M3）+ 批次 2（M4）+ 批次 4 模板。
- 运行模式：**全自动**（D4 决策，2026-08-20）——不加人工介入检查点，保持无人工自动跑通以做干净对照；检查点 2（首个数值证据后检查）先以 M3 确定性反证实现，人工介入版本延后到盲测条件④（16 阶段 1）。
- 命令：`math-agent supervise --problem <MCM-51 spec> --out runs/51mcm-a-brief-v2 --brief problems/mcm51-a/brief.json --thread default`（**新目录，禁止 recover 旧 run**）。
- 验收清单（对照 12 §4.2 + 本批次新增）：
  1. 管道：`brief.json` 副本 + sha256 对上；coverage 全 followed（40 条，含 M6 新增）；
  2. **红线确定性校验（M3）**：`gate_diagnostics.json` redline 段为空；2.2 不再出现 K_avg 整体用法（12 §7 硬伤 1 闭环）；
  3. **对照方案（M4）**：final_state ≥2 个 baseline 证据角色、RESULT 可解析、对比表非空（硬伤「基线对照纯文字」闭环）；
  4. **figure_plan（M2）**：fig6 三线图落地（硬伤 2 闭环）；
  5. 单位口径（M5/M6）：3.2 无 T_VM 应力/力矩混比、1.2 回归方程系数数值出现（硬伤 3/4 闭环）；
  6. LaTeX：`paper.tex` 编译零错误（M1）；PDF 仅预览验证，不要求完整可交付（2026-08-20 定位）；
  7. **测量基线（批次 4）**：cost_summary.md、评审记录、人工投入记录、人工修补清单齐全；
  8. degraded 判定：若仍 degraded，原因必须机器可解释（不出现「原因未知」）。
- 结果落点：写入 12 §七 更新或新评审小节；00 关键数字更新。

## 二、5.2 注入前后抽样对比

- 方法：`scripts/sample_brief_direction.py` 在 brief-v2（批次 3 之前或未接线版本）与 brief-v3（注入后）各跑一次，同一 brief、同一题。
- 对比指标：1.2 变点 / 2.2 D_hole / 3.1 垫圈 / redline 600.71 / redline 0.8·T_max 在蓝图、代码、论文的落地率；required_discussions 出现率。
- 结论判定：落地率无差异 → 阶段 2 结构化传递（13 §三.2）不必要或推迟；有差异 → 立项阶段 2。

## 三、5.3 去参考反事实（17#2，实验设计需先交流确认）

- 前置：先产出 brief 来源拆分表（题面/附件/领域常识/官方答案/人工推导——可从 brief.json + 12 §七 附逐条对照导出）。
- 实验：去掉「官方答案」来源条目后的 brief 短跑 vs brief-v2 对比。
- **注意**：这是「实验方案」决策（怎么定义官方答案来源、跑多长、怎么评），发起前在交流中确认设计，不自行开跑。

## 四、5.4 多种子与 5.5 强模型+工具基线（17#4/#5，均需交流确认后立项）

- 5.4：同一冻结版本 2–3 随机种子（测量方差与偶然命中）——预算约 2–3 × 单 run；
- 5.5：单题试点「强模型 + Python 工具」基线（不经过 Beacon 管线）——判断体系净增益，是 16 阶段 1 盲测的预研。
- 两者都属于 16 阶段 1 盲测的组成，建议与盲测集建设一起立项（07 交流确认），本批次只做 brief-v2 与 5.2。

## 五、预算提示

- brief-v2 单 run 参考 brief-v1（约 15–25 万 tokens / 30–45 分钟）；5.2 额外一次抽样 run；具体预算在发起前确认时给出。
