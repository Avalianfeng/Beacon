# 8-26 落地日志

> 按 `8-26-建议的具体情况.md` 分阶段落地：每完成一个类别，在此追加一段总结并 git 提交。

---

## 2026-08-26 · D 类完成：论文化红线机械化 ✅

**落地内容**：`scripts/check_paper_numbers.py`（标准库 only，中文输出）双功能校验工具，已通过真实材料验证并入库。

- **功能 A（白名单 diff，D1）**：论文/章节 md 中每个数字 token（时间戳 `h:mm:ss` 整体提取、小数/千分位/负号、U+2212 归一、JSON 证据键值全入白名单、整数形浮点归一）必须在 evidence/求解器输出/敏感性 json 中有溯源；未溯源即 WARN 清单（文件+行号+上下文），`--strict` 有未溯源退出码 1，作**进稿闸门**（D2 流程落地）。
- **功能 B（附录 A 溯源核对，D3）**：按真实附录 A 格式（`章节/10-附录.md` 的 `| 论文数字 | 含义 | 来源 |` 表）解析"文件+行号/行号范围"引用逐条断言（支持 `第 N 行`/`第 N–M 行`/斜杠列表/路径继承），失败条目即溯源断链。

**验证结果**（输出存 `.probe_tmp/d-check/`）：
- 13 个 pytest 全过（仓库内 fixture，未用 tmp_path）；
- **污染样本**（`07-6-敏感性分析.md`）：必报 6 数字 **216203/127985/214853/126635/250000/350000 全部报出**，`--strict` 退出码 1——正是论文化阶段凭空造的表 9/表 10 数字，机制生效；
- **干净样本**（轨 B 原稿）：1889 token 中 61 个未溯源全部为噪音类（节号/题面常量/推导值/文献页码），核心求解器数字零漏网；
- **附录 A 溯源核对**：抓出 3 条溯源断链行（声称数字来自 `代码/sensitivity.py` 但脚本无此数字）+ evidence.md 2 条真实引用瑕疵。

**遗留**：噪音放行建议 `--allow 2026,2001,2010,207,249,272`（年份/页码）；题面常量与推导值严格模式下建议补录证据；E1（参考答案录入 calibration）待 D-B 决策。

---

## 2026-08-26 · E1 完成：官方参考答案录入 calibration（D-B ✅）

- 新增 `problems/mcm51-b/calibration/`：`官方参考答案要点.md`（人读：评分结构、四问区间/声明值/购置要求/酌情条款/勘误备注 + 我们 vs 官方对照表与根因指向）+ `official_answers.json`（机读：供 E2 自动口径偏差报告）。
- 关键锚点入库：Q1 区间 37000–39500（声明 37280）、Q2 声明 142965（含 10 小时勘误备注）、Q3 区间 72500–85000（声明 72860，55% 酌情）、Q4 区间 30750–38500（声明 30755，购置 D3+E3 共 7 台 15 分规则）；A1 由 4 输送臂+5 灌装机同时执行的官方口径铁证。

## 2026-08-26 · A 类草案已出（待确认后合入）

- 新增 `docs/实现计划-8-20/8-26-A类草案-brief-playbook口径防线修订.md`：对 brief-playbook.md 的 6 处增量修订草案——①§3.1 假设分级（①原文引用/②合理推断/③经验假设）+ 备选口径与数量级差；②§3.3 禁止性假设裁决纪律（禁"题面没有此机制"式禁止、禁"题面明确要求"表述）；③§3.4 新增"单向约束逆命题"分歧类型；④§4.3 题面信号清单（可加行/设备冗余/类型级vs实例级/工程量守恒）；⑤§5.1 brief 假设条目保留分级标注；⑥文件头 v3 版本说明。含 4 个待确认点。

## 2026-08-26 · A 类完成：brief-playbook v3「口径防线」合入 ✅

- 用户确认草案后，6 处修订全部合入 `docs/brief-playbook.md`（v3 增补）：
  ① §3.1 假设分级（①原文引用带出处 / ②合理推断 / ③经验假设）+ 口径分歧必记裁决、备选口径与数量级差；
  ② §3.3 禁止性假设裁决纪律：禁"题面没有此机制"式禁止、"题面明确要求"只允许分级①，附"为什么是弱依据"推理与 mcm51-b 代价（Q1 +4320s / Q2 +50.6% / Q3 +74.4% / Q4 +302%）；
  ③ §3.4 新增"单向约束逆命题"分歧类型 + 完整解释（资源→任务方向、类型级 vs 实例级、判定两步法）；
  ④ §4.3 题面信号清单（表可加行 / 设备台数冗余 / 类型级表述 / 工程量守恒）逐信号说明"为什么重要"；
  ⑤ §5.1 brief 假设条目保留分级标注（含假设 9 类条目的正确写法示例）；
  ⑥ 文件头 v3 版本说明。
- 表述优化：所有"依赖对例子的理解"处均补充推理链、判定规则与 mcm51-b 实证（用户要求：单句可翻倍，防误解优先）。
- 回填：docs/20 §五 S1 标注已落地；8-26 文档 A 类标注完成。
- 待确认点 4 项全部确认：经验假设进 brief 文本（仅标注）；信号清单先文档化；备选口径暂不进 schema；S1 同步标注。

## 2026-08-26 · 已知事项：文档体系清理归档（待后续专门处理）

- 用户指出：目前跑过的案例教训（docs/20、docs/18、实现计划-8-20/实际操作记录/、archive/ 等）**混着存放，虽有基本管理但整体混乱**；后续必须重新考虑**清理、归档、新建**文档体系（统一索引/分层/命名）。本次未动，待专门批次处理（建议列入下个计划周期）。

---

## 2026-08-26 · C 类完成：评审层口径核查工具 ✅（流程注入待确认）

- **C1** `scripts/check_assumption_claims.py`（子 agent 实现 + 独立验证）：R1 同段自相矛盾检测（同谓词成对判定，"题面明确要求/假设" vs "题面未要求/未假设"）；R2 "题面明确要求"声明清单（引号标记辅助人工核对）；R3 假设条目完整性。14 测试全过。
  **验证发现（新事实）**：轨 A 论文同框矛盾共 **3 处（假设 7/9/10）**，均无题面原文依据；Word 稿同 3 处；轨 B 原稿 0 处（诚实标注）。docs/20 此前只记录假设 9，已补全。
- **C2** `scripts/check_gap_trigger.py`（主 Agent 实现）：从证据 json 递归找"下界+ makespan"配对，gap>15% 输出口径核查模板（禁免责声明、要求机制性解释+口径核查）。10 测试全过；真实 sensitivity.json：Q2 74.17% 触发、Q1 0.48%/Q3 2.81% 正常。
- **C3** `docs/实现计划-8-20/8-26-C类-评审口径核查必答项.md`：评审四必答项（假设逐条核查 / 大 gap 根因调查 / 数字溯源与敏感性口径 / 得分点覆盖）+ 输出模板 + 4 个待确认点。**流水线 prompt 注入待模板确认后实施**（涉及回归测试）。

---

## 2026-08-26 · B 类改版完成 + E2/F/G 落地 ✅

- **B 类改版（用户战略确认）**：求解重心 = agent 现写脚本 → 为已赛完的 reference_solver 做参数化不再必要；
  B 类三件事（口径显式化/备选量化/可目检）改版为**轨 B 求解脚本产出规范（五条）**，并入 docs/18
  轨 B 提示词要点：①口径参数化（假设做顶层参数禁写死）②分歧双跑（备选口径两组数字+对比表）
  ③诊断明细（每工序耗时/利用率/瓶颈 + RESULT 统一）④evidence 格式（数字→文件+行号）⑤哈希登记
  （sha256 锚点，修改须重登记重估）。D-A 验证性重跑降级为可选一次性实验（根因已由官方表 1 + 4320s 差分实锤）。
- **E2** `scripts/report_calibration_diff.py`：官方 calibration vs 我方数字自动对照（区间命中/声明偏差/购置对比）。
  9 测试全过；真实输出：q1 +11.6% / q2 +50.6% / q3 +74.4% / q4 +302.7%，购置 2 vs 7 台 ❌，未命中 4 处。
- **F** `8-26-F类-得分点清单模板.md`：通用模板 + mcm51-b 实例 14 项（8 项 ❌ = 39 分结果分来源）+ 细则不可得时
  假设性得分点 5 项（F2）+ 赛后回填流程。
- **G**：双轨实验约定（独立口径复核 / 同源盲区声明）并入 docs/18。
- 8-26 文档：B/E/F/G 全部标注完成；实施顺序更新。

---

## 2026-08-26 · C3 流程注入完成 + F 类重定位 + 执行原则确立 ✅

- **C3 注入（用户确认全按草案）**：口径核查四必答项（假设逐条核查 / 大差距根因 / 数字溯源与敏感性口径 /
  得分点覆盖）已注入 `src/math_agent/prompts/evaluation.py` 与 `paper_critic.py`（SYSTEM 追加，schema 不变），
  轨 B 评审 Agent 清单同步更新（docs/18）；**264 个节点测试全过**（含 paper_critic 17 + evaluation 相关）。
- **F 类重定位（用户质疑）**：正常流程不会有官方评分要点 → 原"细则拆得分点清单"不成立。
  重定位：得分点检查 = 题面信号驱动的交付物完整性检查（5 条），作为**轨 B 写作/评审 agent 提示词的一条**，
  体系侧不建独立工具；细则拆解仅赛后校准场景（E1/E2）；评审必答项 Q4 已覆盖。`8-26-F类-得分点清单模板.md` 已重写。
- **执行原则（用户确立，后续所有工作须意识到）**：① 修改要贴合真实使用流程；② 后面脚本是 agent 写，
  **能在 agent 侧完善的就在 agent 侧完善**（求解规范、得分点检查、口径核查均如此），体系侧做需**明确价值**；
  ③ 当前阶段 = 前置修复（已完成 A/B/C/D/E/F/G）+ **先走整个体系（实验能否直接跑通）**；
  ④ 已有随时中断恢复能力 → **子 agent 完整产出与体系一起在主会话管理之下**，后续再简化流程。

## 2026-08-26 · 8-26 清单全部落地 ✅

A（brief-playbook v3 口径防线）/ B（轨 B 求解规范五条）/ C（三工具 + 四必答项注入）/ D（数字红线校验）/
E1/E2（calibration + 偏差报告）/ F（重定位 agent 侧）/ G（双轨约定）全部完成并提交
（5bf99ce → 06029d5 → 403bd57 → 96c43d0 → 79e7495 → 待提交 C3/F 重定位）。

---

## 2026-08-26 · 决策落档：run6 抛弃 + D-C 延迟；M3 说明；大模块规划；文档清理方案

- **run6 处置 = 抛弃**（用户拍板）：degraded 终态 + G10b 数值含旧 bug 无效，随新题重跑（07 行 19a 已标注）。
- **D-C 落档延迟**至新题完整跑完后（双轨结论依赖新流程有效数值）。
- **pilot --pilot CLI 不做**、**M2 figure 注入不做**（并入图体系重设计）；**外部使用流程暂不处理**（留待统一处理）。
- **M3 红线机器化**：写了说明 `8-26-M3红线机器化说明.md`——是什么（brief 红线文本→机器规则→产物确定性校验，
  违反 retry/stop，不再靠 LLM 压线，B16 实证）、rule_type 五种、接入点、价值/局限；建议"最小版+新题实测"
  或"暂缓按缺口定"二选一，**不阻塞全流程**。
- **大模块规划** `8-26-大模块规划-必须做但不着急.md`：图体系重设计（🟥）、流程使用体系（🟥，暂缓）、
  文档清理（进行中）、求解规范验证演进、成本质量基线（H06）、盲测集治理（H16）、E3 模式库、论文化流程化、
  H 前置选型——各含动机/范围/触发条件，**不排期，等用户过目定优先级**。
- **文档体系清理方案** `8-26-文档体系清理方案.md`：盘点 + 目标结构（18=可迁移范例/20=赛后复盘/实际操作记录=
  只读证据）+ 操作清单（A 安全 4 项 / B 移动归档 5 项 / C 删除合并 3 项）——A 类可立即执行，B/C 待确认。

---

## 2026-08-26 · C 题启动：开场总结已落档

- 用户确认跑法：**主会话统一管理 + 子 agent 分工**（探索/数据探查/求解/写作/评审），不跑轨 A 流水线，
  多模型对撞用"多角色分工+独立产出"近似；文档清理交另一 agent（本次不管）。
- 新会话开场总结落档：`8-26-C题-开场总结.md`（必读清单/体系现状/工具闸门/执行方案/C 题要点/上次教训/
  环境纪律/本次边界）。
- 下一步：新会话按开场总结执行——导入 mcm51-c → 数据探查 → 探索 → brief → 求解 → 评审 → 论文 → 论文化。

---

## 2026-08-26 · C 题（边坡预警）全流程跑通 ✅

- 按开场总结执行方案跑通 2026 五一赛 C 题：① import → ② 数据探查（`problems/mcm51-c/data_profile.md` 280 行 EDA）→ ③④ 开放探索+消化（`exploration.md` + `实际操作记录/第一论-C.md`）→ ⑤ 多角色对撞（deepseek 子 agent + GPT 第二家[用户手动跑]+ 主会话第二读者 `口径风险-第二读者.md`）→ ⑥ 逐问清点（`逐问清点.md` 113 行，问题分解表 9 小问）→ ⑦ brief（`brief.json` 47 条，`brief check` [OK]，口径防线 v3 全应用）→ ⑧ 求解（3 并行子 agent，Q1–Q5，纯 numpy/scipy，B 类五条）→ ⑨ 评审（三闸门 + C 类四必答项）→ ⑩ 轨 B 论文（`runs/mcm51-c-skill-v1/paper.md` 351 行，零编造）→ ⑪ 附录 B 代码清单（9 脚本+sha256）。
- 关键机制实战验证：口径防线 v3（假设分级/弱依据禁令/题面信号清单/备选口径）全程落地；主会话**独立锚点交叉核验**抓到 Q1 子 agent 的 β1 退化 bug（整模型 RMSE=2mm 但表 1.1 四点偏差 26–184mm）并 `interrupt_agent` 返工，最终增益 0.88 模型表 1.1 偏差 ≤0.038mm；`check_assumption_claims` 0 矛盾；`check_paper_numbers --strict` 60 未溯源核验为附录 A 行号误报+派生值（非造数）；评审四必答项 4 处表述级修订后放行。
- 过程记录 + 流程操作/迭代经验见 `docs/8-26-C题记录.md`（批次 1–7）。

## 下一步

- 按 `docs/8-26-C题记录.md` 批次 4/6 的迭代方向（分段书写 / 最小论文 / 参考实现登记+自动论文装配 / 环境预检前置）规划下一轮流程优化。

---

## 2026-08-27 · D-002 混合路线拍板

- 用户确认：下一阶段主线 = 流程介入面重构（人/外部 agent 按阶段驱动）；LangGraph/证据链/`reference` 降为工具层，不重写架构。
- 落地：`8-27-流程介入面计划.md`；07 D-002 accepted；大模块 2 解冻（Web/`brief extract`/H14 仍不自动解冻）；current/01–03 与 00 §六已同步。
- 第一刀：P0 操作面契约（不改代码）。

## 2026-08-27 · P0 操作面契约定稿

- 交付 `8-27-操作面契约.md`：S0–S9 按真实 CLI/脚本书写；缺口 T-01–T-20 标【待实现】，不发明做法。
- C 题产物可逐阶段对号。下一刀 P1 只从待实现表挑。

## 2026-08-27 · P1 命令收口完成

- D-003：`review-check` 仅包装 check_*（根命令 `review` 仍为人审）。
- 代码：`ops_stage` / `ops_preflight` / `ops_review` / `ops_verify` / `ops_help`；CLI 接线。
- 验收：相关 pytest 78 passed；`problem stage mcm51-c` → completed S0–S3，next S4（缺 preflight.json）。
- 下一刀 P2 介入最小闭环。

## 2026-08-27 · P2 介入最小闭环完成

- D-004：T-09/T-17 为登记入口，不写 LangGraph checkpoint，不做 docs/19 全量 checklist。
- 代码：`ops_handoff` / `ops_recertify` / `ops_accept` / `ops_gate`；S6 paper/tables 闸门；`reference add` 同源只登记。
- 验收：相关 pytest 102 passed；mcm51-c 走通 S0–S8（`runs/mcm51-c-p2/`，不覆盖 skill-v1）；`problem stage` completed S0–S8，S9 optional。
- 下一刀 P3 新题验证。

## 2026-08-27 · P3 新题验证完成（D-C 证据）

- 题：2026 MathorCup C（`mathorcup16-c`，中老年高血脂预警）；非 mcm51-*。
- 操作面 S0–S8：`problem stage` completed S0–S8。产物 `problems/mathorcup16-c/`；装配 `runs/mathorcup16-c-p3/`（evidence + tables + 骨架 paper）。recertify `--rerun` 数值一致。
- 对照 supervise 烟测：`runs/mathorcup16-c-p3-s9/`，`--max-recoveries 1`；analyst→…→coder_generate/execute 循环后 `pause` 停在 `coder_execute`（约 24 次 LLM、completion_tokens≈44066）。**未 recover、未出论文**。
- 新缺口（不本拍实现）：论文骨架摘要仍用 mcm51-c 默认模板（T-21）；`brief.problem_id` vs 中文 title 误报不匹配；tables 仍提示 C 题表3.2。
- **D-C 建议（待用户拍板）**：维持 D-002——默认分阶段操作面；supervise 可到达编码但本烟测未稳定出数即进入 generate/execute 循环，不能当默认主路径。体系相对纯 skill 多出来的是闸门、哈希、`next_command`、机械三项 verify。

## 2026-08-27 · D-005 D-C 收口

- 用户确认按建议走：默认操作面；supervise 不作主路径。
- 07 行 19a 指向 D-005；不重开架构、不把轨 A 出好论文拉回。

## 2026-08-27 · P4 S6 题级卫生

- T-21：非 `mcm51-c` 不再套用边坡摘要/表1.1；按小问 + evidence 键生成；求解结果按 `questions` 长度。
- S6 完成须 runs 下解析到 `paper.md`；`next_command` 在 `runs/<id>-*` 中取最新 evidence/paper。
- B04：`brief.problem_id == spec.problem_id` 短路，mathorcup16-c 中文 title 不再误报。
- 验收：重装 `runs/mathorcup16-c-p3/paper.md`，无边坡/q4/q5/问题四；pytest 83 passed（P4 相关）；`problem stage` 仍 S0–S8。

## 2026-08-27 · P5 L4 闸门（D-006）

- 五一 C 诊断 §6 规则 1–4 进操作面：`scripts/check_l4_gates.py`（G1 敏感性回流 / G2 y0 / G3 异常操作定义；G4 解释失败仅 WARN）。
- `review-check` 有 `--paper` 时增挂；不改前三脚本；不进 `graph.py`。
- C 类必答项加 Q5；契约 S1 建议概览图、S2 知识卡片进 `background_knowledge`、S5 L4 纪律、S7 第四脚本。
- 规则 5（方法名≠对错）不机械化。T-02/T-03/T-11/T-20 仍挂。

## 2026-08-27 · 内外全流程总纲 + 题目档案赛后段（D-007）

- **`docs/内外全流程总纲.md`**：端到端一张图——内（操作面 S0–S9）↔ 外（D 盘工作面 S0–S7）↔ 教训回流
  （诊断 §6 规则 → Beacon T-xx 闸门，L4/D-006 为第一例）；环节表（产物/执行者/完成判据）；
  交接点 = paper.md + evidence-package + 关键数字.md（数字唯一来源）；不再凭感觉。
- **b/c/d 升级强制产物**：S1 data_profile.md 须含每附件概览图路径（D-007）；S2 须产出 `领域知识.md`
  并写入 brief background_knowledge（操作面契约 S1/S2 修订）。
- **题目档案补赛后段**（操作面契约 §0.1）：赛后诊断.md / 教训卡片.md / calibration/ / 关联题交叉引用；
  旧题不补，mcm51-c 已补作范本（赛后诊断摘要+指针 / 教训卡片 E1–E5 / 领域知识卡片 / 官方细则转录）。
- D-007 已登记 07；控制面 current/01、04 与 docs/README 索引同步；audit_docs.py 0 非豁免缺陷。

## 2026-08-27 · P6 操作面认知卫生

- 根 help（`ops_help.ROOT_HELP`）默认只列做题主路径 S0–S8；S9 标「可选遗留执行器」；ingest/bench/brief dialogue 标非做题路径。
- `problem stage` JSON/文本增 `d007_missing`（缺 `领域知识.md` / data_profile 未列概览图）；**不改** S1/S2 完成门槛，旧题不掉级。
- 不删命令、不重写 graph、不新写流程 v3。契约 T-25；总纲与 8-27 计划已同步。


