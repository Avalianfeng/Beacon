# 大模块规划：必须做但不着急（2026-08-26）

> **用途**：登记"体系必须做、但不着急、暂不排期"的大模块——用户 2026-08-26 提出（图体系、流程使用体系等），
> 本文补全其余候选。**只登记范围与动机，不做排期**；用户看完后决定优先级与触发条件。
> 状态约定：🟥 用户点名 / 🟨 我补的候选 / ⬜ 未定。

---

## 1. 图体系重设计 🟥（用户点名；"暂时不考虑"）

- **动机（mcm51-b 实证痛点）**：
  - 甘特图几乎不可读：颜色偏深（#4C72B0）、每根 bar 标注设备 id、27 活动挤一张图（docs/18 §六 轨 B 观察）；
  - 图缺失/错配历史：fig6 缺失（B17）、正文图 10 `device_movement.png` 无对应物（R15）、
    A/B 图以手绘 jpg 替代；
  - M2（brief figure_plan 注入 figure 节点）未实现——图生成没有按 brief 计划的强约束；
  - 图视觉再生产不在当前流程（用户此前确认"图视觉再生产不在本流程"）。
- **范围**：图生产规范（draw 样式/配色/标注规则/单图容量）、图-正文引用一致性校验、
  图证据链（图文件与数字同源）、图类型库（甘特/预算曲线/敏感性/流程图）——与 M2 合并考虑。
- **状态**：暂缓（用户：暂时不考虑，需要重新设计）。
- **触发条件**：新题跑通后按实测图缺陷设计；或提交前统一处理（与 18 文档 P2/P3 图建议同批）。

## 2. 流程使用体系 🟥（用户点名；"涉及外部使用流程的暂时不处理，留待后续统一处理"）

- **动机**：外部使用流程 = 用户如何用 Beacon 完整做一道新题（导入 → 探索 → brief → 跑 → 论文化 →
  提交）——当前 CLI 是机械件、双入口未实现（入口 B 的 `brief extract` 方向已定）、Web UI 简化计划暂缓、
  H14 新手适配未做（入口按用户成熟度自适应）。
- **范围**：外部流程文档化（用户视角 runbook）、双入口（brief extract）、Web UI（10 简化计划）、
  CLI 用户面完善、H14 自适应入口。
- **状态**：**暂缓，留待后续统一处理**（用户 2026-08-26 明确）。
- **触发条件**：体系内部跑通后（先内后外）；或用户明确要求。

## 3. 文档体系清理归档 🟨（进行中——见 `8-26-文档体系清理方案.md`）

- 动机：案例教训混着存放（docs/20、18、实际操作记录/、archive/ 边界不清）、批次状态标注不一致、
  8-26 系列命名未入编号体系。
- 状态：方案已出，待用户过目后执行。

## 4. 求解规范与证据体系验证演进 🟨

- 动机：轨 B 求解脚本规范五条（口径参数化/分歧双跑/诊断明细/evidence 格式/哈希登记，docs/18）已立，
  但**未在新题真跑中验证**；8-26 四个 check 工具（check_paper_numbers / check_assumption_claims /
  check_gap_trigger / report_calibration_diff）散在 scripts/，后续可整合为统一评审工具/命令
  （"后续简化流程"时做，用户此前确认不做 CLI 评审命令）。
- 范围：新题真跑中按规范执行并修订；工具族整合为 `review-check` 类命令（可选）。
- 触发条件：新题真跑时自然验证；整合在新题跑通后。

## 5. 成本与质量基线 🟨（H06）

- 动机：成本遥测 v0 已有（brief-v1 实测表），但无单位经济模型——不知道哪些调用产生边际价值；
  继续加 critic 会放大不可见浪费（15 文档 §一 6）。
- 范围：成本-质量基线、边际价值分析。
- 触发条件：16 路线图阶段（盲测/Go-No-Go 前）。

## 6. 盲测集/测试集治理 🟨（H16）

- 动机：训练—测试污染（MCM-51 已污染）；最大盲区 = 缺真正测试集（15 文档 §一 9⑧）。
- 范围：3 道干净题 + 输出冻结后自评（16 阶段 1 已降级版）。
- 触发条件：16 路线图阶段 1。

## 7. E3 命题者意图模式库 🟨

- 动机：官方参考答案的区间形态/购置偏好/细则算法提示学习（如官方 2.1(2) 贪心描述暗示分摊口径）；
  H2 类"特设答案命中"的先验来源。
- 范围：≥2 题赛后校准后建库（calibration 资产已具备 mcm51-b 第一例）。
- 触发条件：≥2 题（当前 1 题）。

## 8. 论文化流程化 🟨

- 动机：论文化（Word 化）是证据链断裂点（表 9 造数实证）；D 类已做进稿闸门机械校验，
  但论文化本身无流程/工具（编辑手 + 人工粘贴）。
- 范围：论文化 SOP（含 D 类校验闸门）、附录 B 代码自动装配（本次附录 B 留空扣分风险）。
- 触发条件：新题提交前必须闭环（交付类风险最高）。

## 9. H 前置智慧选型 🟨（非代码）

- 动机：口径裁决/特设答案命中只能靠前置输入质量（docs/20 §四 H1–H3）；
  每次做题的"人 + 模型 + 外部经验"选型没有流程。
- 范围：做题前置选型 checklist（双人双读/人机分读/历史真题参照/细则前置收集）。
- 触发条件：每次新题开工前（可并入 brief-playbook 使用说明）。

---

## 附：用户 2026-08-26 明确不做的项（记录防复燃）

- pilot `--pilot` CLI：不做（手工剧本可用；pilot 检查可由主会话/子 agent 承担）；
- M2 figure 注入：不做（并入图体系重设计，见模块 1）；
- M3 红线机器化：不排期（说明见 `8-26-M3红线机器化说明.md`，最小版或暂缓二选一）。
