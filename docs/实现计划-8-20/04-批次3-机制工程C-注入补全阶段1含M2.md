# 批次 3：机制工程 C — 注入补全阶段 1（含 M2）

- 状态：待办（依赖批次 0；schema 相关并入批次 1 设计）
- 目标：把 docs/13 第一节矩阵中的四个「无直接注入点」字段接到目标节点，消除 analyst 转述衰减。
- 依据：B06、B17（fig6 缺失实证）、13 §三.1、12 §八 M2。

---

## 一、任务清单（按 docs/13 三.1 表逐项）

### 3.1 M2：figure 环节注入 figure_plan（B17 闭环）

- 现状：figure 生成 prompt（`src/math_agent/prompts/coder_figure_one.py`、figure_pipeline）看不到 figure_plan；brief-v1 计划 8 张图只落地 2 张，fig6（T_max-e 三线图，3 分）缺失归零。
- 改动：
  1. `src/math_agent/brief.py` 新增 `render_figure_brief(brief, question_ids)` —— 渲染 figure_plan 子集（按 question_id 过滤、每条 ≤200 字符、整块 ≤1500）；
  2. 接线到 figure 环节 prompt（coder_figure_one.py 主 prompt 与 figure_pipeline 生成路径）；
  3. 容量控制 + 无 brief 不含块。
- 测试：参数化（有 brief 含块 / 无 brief 不含块；容量断言）。
- 真跑判据（批次 5）：论文出现 fig6 对应三线图（对应 12 §7 残留硬伤 2 闭环）。

### 3.2 evaluation / paper_critic 注入 scoring_notes

- 改动：新增 `render_scoring_brief(brief, question_ids)`（评分要点+容差子集）→ 接线 evaluation 与 paper_critic 评审 prompt（两处各自渲染）。
- 测试：同 3.1 模式。

### 3.3 modeler / coder 扩展 data_notes

- 改动：`render_modeler_brief`（brief.py:263）/ `render_coder_brief`（brief.py:284）扩展或新增块，并入 data_notes（按题目过滤、截断至 ≤200 字/条）。
- 测试：同 3.1 模式。

### 3.4 writer references 注入 reference_direction

- 改动：`render_discussions_for_group`（brief.py:332）扩展或独立块，把 reference_direction 按 section 分组注入 writer_section references。
- 测试：同 3.1 模式。

## 二、统一约束

- 全部渲染器为纯函数（不调用 LLM）；「有 brief 含块 / 无 brief 不含块」逐字节兼容（test_brief.py 模式）；
- 容量：每条 ≤200 字符、整块 ≤1500 字符级（13 §四）；
- 不动节点业务逻辑语义，只加 prompt 块；
- 不注入参考答案数值（原则 1）。

## 三、测试与验证

- 单测：每个新注入点参数化（含块/不含块/容量）；子集 pytest 全绿。
- 真跑对比（批次 5）：`scripts/sample_brief_direction.py` 注入前后各跑一次，对比 1.2/2.2/3.1/4 方向在蓝图、代码、论文的落地率与 required_discussions 出现率。
- 阶段 2（结构化传递，ProblemBlueprint 显式转述字段方案 A）**依本批次真跑效果决定**，不进本批次。

## 四、风险

- prompt 变长稀释注意力（容量控制缓解）；evaluation/paper_critic 看到评分要点后可能「照抄要点话术」——评审 prompt 明确「要点用于核对，不得直接复述」（观察真跑）。
