<!-- doc: type=plan status=active updated=2026-08-29 -->
# 验证内容集：D-022（brief 硬信号）+ D-023（重试哲学一次停）

> **背景**：2026-08-29 两轮大改造落地（D-022 四 commit 已提交：8a60341/81711f6/86ed477/3b40f2d；D-023 工作区未提交）。本文档为后续重新跑验证提供依据：每层含「命令 + 预期 + 实施时基线」。执行结果记入 `运行记录-YYYY-MM-DD.md`。
> **快速判断**：先跑第一、二层（无 LLM，~5 分钟）；第三层需 LLM（预算敏感，按需）。

---

## 改造范围速览

| 改造 | 内容 | 影响面 |
|---|---|---|
| D-022 | 表驱动注入（NODE_BRIEF_SLICE/render_slice/golden）、schema v2 + redline_rules、redline_violations 双端（S9 hard 停 / S7 check_redlines）、S7 挂 check_brief_claims + brief_sha256、_jsonable 不截断、blueprint_gate_problems 定向反馈、prompts/coder.py 死代码清理、S6 双轨统一 | brief.py、prompts/*、nodes/*、routing、ops_review/handoff、cli、insight、paper_expand、mcm51-c brief.json |
| D-023 | 四道门禁（blueprint/model/paper/model_code_consistency）首次未过即 stop；废除 advance_with_warning/advance_review；_gate_stop_reason 覆盖全部门禁；review 命令接管 paper 停机；restart --from coder 接续一致性停机 | routing.py、cli.py（review/restart/resume）、state.py（paper_review_takeover）、graph.py、model_code_consistency |

---

## 第一层：单元/集成测试（无 LLM · 必跑）

```powershell
# D-022 相关（实施基线 166 passed）
uv run pytest tests/test_brief.py tests/test_routing.py tests/test_insight.py tests/test_ops_review.py tests/test_ops_handoff.py tests/test_restart.py tests/test_coder_baseline.py tests/test_cli.py -q

# D-023 相关（实施基线 141 passed）
uv run pytest tests/test_routing.py tests/test_restart.py tests/test_pause.py tests/test_graph_gate.py tests/test_cli.py tests/nodes/test_coder.py tests/test_graph_smoke.py tests/test_llm.py -q
```

预期：全绿；重点抽查项——`test_brief`（渲染 golden 逐字符对照、schema v1/v2 双读、redline_rules id 关联校验）、`test_routing`（四门禁 stop 语义、_gate_stop_reason 覆盖）、`test_restart`（输入不变性拒绝路径）。

## 第二层：CLI/脚本冒烟（无 LLM · 必跑）

| # | 命令 | 预期 |
|---|---|---|
| 2.1 | `uv run python -m math_agent.cli brief check --brief problems/mcm51-c/brief.json` | `[OK] schema_version=2`、51 条待回应、`redline_rules：7 条` |
| 2.2 | `uv run python -m math_agent.cli brief check --brief problems/mathorcup16-c/brief.json` | v1 兼容读通过（无 redline_rules 提示） |
| 2.3 | v1 含 redline_rules 的提示：临时构造 `{"schema_version":1,"redline_rules":[]}` 跑 brief check | `[OK]` 通过 + `[WARN] schema_version=1 含 redline_rules，请升至 schema_version=2`（**WARN 不阻断**，2026-08-29 实测确认） |
| 2.4 | `uv run python scripts/check_redlines.py --brief problems/mcm51-c/brief.json --code tests/test_brief.py` | 检出 warn（如 rl-leak），退出码 0（warn 不阻断） |
| 2.5 | 构造 hard 违规样例（rl-result-format pattern 命中）跑 check_redlines | 退出码非零（hard 不论 --strict） |
| 2.6 | `review-check --paper <样例> --evidence <样例> --brief problems/mcm51-c/brief.json` | review-report.json 含 `brief_sha256` + check_redlines/check_brief_claims 结果块 |
| 2.7 | 对非停机态 run 执行 `restart --from coder` | `[REJECT] checkpoint 未处于停机态` |
| 2.8 | 对终态 run 执行 `restart --from coder` | `[REJECT] run 已终态完成` |
| 2.9 | 改 out/brief.json 后 restart | `[REJECT] 输入已变，只能全新 run` |
| 2.10 | `uv run python scripts/check_brief_claims.py --brief problems/mcm51-c/brief.json --paper <样例>` | 图/数据口径核对清单输出 |

> 样例产物优先复用 `runs/` 现有 run（如 `runs/mcm51-c-refA`、`runs/51mcm-a-brief-v1`）；检查其 review-report.json 是否含 brief_sha256（旧产物无 → 属预期，新跑才有）。

## 第三层：真实 LLM 冒烟（prompt 变更面 · 按需/预算敏感）

> 背景：D-022 改了 5 处 prompt 注入（modeler 收红线、coder/coder_baseline 收 data_notes、evaluation/paper_critic 收评分点）——LLM 行为不可单测，必须真跑。D-023 后门禁首次未过即 stop，接续路径也需演练。

| # | 场景 | 验证点 | 预期 |
|---|---|---|---|
| 3.1 | mcm51-c supervise/start 全流程（或小案例） | modeler prompt 含红线块；coder/baseline prompt 含 data_notes；无 brief 时仍恒通过 | 流程不炸、切片正确注入 |
| 3.2 | 停机演练 A：制造 paper_critic 未过 → `math-agent review` | 置位 paper_review_takeover → advance → table_assembler → evaluation → human_review 前暂停；`resume --approve/--no-approve` 可推进 | 人工接管路径通 |
| 3.3 | 停机演练 B：制造一致性未过（或 hard 红线）→ `math-agent restart --from coder --reason ...` | _gate_stop_reason 显示正确原因（hard 红线/无主证据/未达门槛）；restart 从 coder 重试；输入不变性校验生效 | 断点续跑路径通 |
| 3.4 | 停机提示面：stop 后 `supervise`/status 显示停机原因 | `_gate_stop_reason` 覆盖 5 类原因文案 | 用户可清晰判断下一步 |
| 3.5 | **红线 warn 观察数据收集** | 记录 check_redlines 在真实代码上的命中/误报 | 为 warn→hard 升级与 pattern 收敛提供依据（写进运行记录） |

## 第四层：操作面主路径回归（无 LLM · 按需）

| # | 命令 | 验证点 |
|---|---|---|
| 4.1 | `reference paper/expand --brief problems/mcm51-c/brief.json`（S6） | 双轨统一后 S6 仍读 required_discussions/per_question_direction（假设节/方向行正常） |
| 4.2 | `ops_handoff` S6/S7 交棒 | 交棒命令带 `--brief`（D-022 修掉隐式缺省） |
| 4.3 | `review-check`（S7）完整链 | 四 check + 两新 check + brief_sha256 全部入 report |
| 4.4 | `accept`（S8） | acceptance.json 正常（brief 关联不加，保持现状） |

## 第五层：文档治理（必跑）

```powershell
uv run python scripts/audit_docs.py
```

预期：本区文件（docs/验证/）不在孤儿清单（README 已索引）；D-022/D-023 相关 docs 改动无 frontmatter 违规。

---

## 记录模板（运行记录-YYYY-MM-DD.md）

```markdown
<!-- doc: type=log status=active updated=YYYY-MM-DD -->
# 验证运行记录 YYYY-MM-DD

| 验证集 | 层 | 项 | 结果 | 产物/证据路径 | 备注 |
|---|---|---|---|---|---|
| D022-D023 | 1 | pytest 全组 | pass/fail | — | 基线 166/141 |
| D022-D023 | 2.4 | check_redlines warn | pass | 输出贴文 | 命中 rl-leak |
| ... | ... | ... | ... | ... | ... |
```

## 相关

- 改造详情：`docs/adr/D-022-brief硬信号与schema-v2.md`、`docs/adr/D-023-重试哲学一次停.md`
- 探查全貌：`全流程分析/brief真实调用检查.md`、`全流程分析/brief检查/01–06`
- 待办关联：`全流程分析/后续行动清单.md` 后续建议（真实冒烟 3.1–3.5 即建议 ①）
