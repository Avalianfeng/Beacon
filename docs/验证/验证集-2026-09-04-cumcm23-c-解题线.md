<!-- doc: type=plan status=active updated=2026-09-04 -->
# 验证集 · 2026-09-04 · cumcm23-c 解题线（学-20/21）

> 对撞定稿后 → **长研究会话**（可 Diff 续跑）→ brief 草稿 →（可选）brief check。  
> 论文链本轮不验收。定稿：`全流程分析/全流程定稿/91-验证站.md` · `10附-一次长会话研究.md`。

## 分层验证项

### 0. 前置资产

| ID | 命令或检查 | 预期 |
|---|---|---|
| P0 | 打开 `全流程分析/全流程定稿/README.md` 00–08 为 ☑ | 通过 |
| P1 | `problems/cumcm23-c/认知地图集/清点/{00-拍板表,主线,逐问清点,领域知识}.md` 存在 | 通过 |
| P2 | `problems/cumcm23-c/_派发/研究.md` 为学-21 研究会话稿 | 通过 |
| P3 | Diff：`认知地图集/diff/diff.md` + `_预构想.md` | 通过 |
| P4 | `全流程分析/prompts/研究/` 与定稿 `10附` 存在 | 通过 |

### 1. 研究交付（人跑完后 · 题根）

| ID | 检查 | 预期 |
|---|---|---|
| B1 | 题根 `_研究日志.md` 有多段追加（含失败/推翻） | 通过 |
| B2 | 题根 `解题说明.md` 覆盖 Q1–Q4（不要求机械顺序痕迹） | 通过 |
| B3 | 题根 `经验与坑.md` 非空 | 通过 |
| B4 | 题根 `brief草稿.md` 含 direction/red_lines/data_notes 等；且明显后置于研究 | 通过 |
| B4b | 题根 `exploration.md` 为人读收束（非空壳/非对撞地图代餐） | 通过 |
| B5 | 关键结果表/图可打开，路径在日志或说明中可追 | 至少一侧可打开 |
| B6 | 抽 3 个关键数字：能指到脚本/csv；无全文搜题解痕迹 | 通过 |
| B7 | 若推翻拍板：日志含原判断/证据/新方向；非默默换故事 | 通过 |

### 2. brief 收口（主会话，研究后）

| ID | 命令 | 预期 |
|---|---|---|
| C1 | 从草稿生成/填写 `problems/cumcm23-c/brief.json` | 文件存在 |
| C2 | `math-agent brief check --problem …` | OK 或缺口列表进运行记录 |

### 3. 机制回归（可选）

| ID | 命令 | 预期 |
|---|---|---|
| M1 | 旧集 D-022/D-023 | 绿即可；本轮可不跑 |

### 4. Beacon 证据链（S4–S5 · 人闸视为过之后）

| ID | 命令或检查 | 预期 |
|---|---|---|
| S4 | `uv run math-agent run --dry-run --problem problems/cumcm23-c/problem.json --out runs/cumcm23-c-preflight` | `preflight.json` `ok=true`，`blockers=[]` |
| S5a | 修 `研究/beacon_ref/_entry.py` 题根解析后 `reference add --force` | 登记入口读 `研究/data` |
| S5b | `uv run math-agent reference run --problem problems/cumcm23-c/problem.json --out runs/cumcm23-c-reference` | `evidence.json` 有 `RESULT`；Q3 `n=28` |
| S5c | `uv run math-agent reference verify --problem problems/cumcm23-c/problem.json --evidence runs/cumcm23-c-reference/evidence.json` | `evidence-package.json` `ok=true` |
| S5d | `uv run math-agent reference recertify … --verdict pass` | `independent-review.json` `verdict=pass` |
| S5e | `uv run math-agent problem stage cumcm23-c --json` | `completed` 含 S5；`next=S6`（缺 `paper.md`） |

S6+（`reference tables/paper/expand`、`accept`、无 `--dry-run` 的全图 `run`）本集不验收，等人定。

## 基线（开跑前）

方向段已落盘；**研究已收束**；brief check OK。人闸按指示视为通过后：S4 PASS；S5 曾因 `_entry` 无 `__file__` 卡住，修 `data_dir` 后通。

## 相关

- 派发：`problems/cumcm23-c/_派发/研究.md`
- 续跑：`全流程分析/prompts/研究/说明-从Diff续跑.md`
