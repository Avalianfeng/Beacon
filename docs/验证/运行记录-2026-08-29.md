<!-- doc: type=log status=active updated=2026-08-29 -->
# 验证运行记录 2026-08-29（C 类补测 + 红线离线观察）

> 对应验证集：`验证集-2026-08-29-D022-D023.md`。本轮以「补测试 + 离线冒烟」方式覆盖 C 类（真实 LLM 冒烟暂不真跑，机制面先补测）。

## 1. _gate_stop_reason 全分支补测（对应验证集 3.4 停机提示面）

- 现状缺口：`test_cli.py` 原仅有 brief_coverage / coder_llm_disabled 两个 reason 测试。
- 本轮补 8 个（`tests/test_cli.py`）：hard 红线违规、code_verify 无主证据、code_verify 未达门槛、blueprint_critic 未通过、model_critic 未通过、论文关键 section 为空、paper_critic 未通过、quality gate 停机（兜底）。
- 结果：`uv run pytest tests/test_cli.py` → **59 passed**（51 旧 + 8 新）。

## 2. 红线 warn 离线观察（对应验证集 3.5，不烧 LLM）

- 方法：从 `runs/*/final_state.json` 提取最新 batch 的 code_artifacts 真实代码 → `check_redlines --brief problems/mcm51-c/brief.json --code`。
- 样本与结果：

| run | batch/文件数 | 命中 | 说明 |
|---|---|---|---|
| runs/mcm51-c-refA | 1 / 5 | 0 | C 题真实代码，时序切分符合 rl-leak |
| runs/mathorcup-c-2026-cli-exp-v2 | 4 / 8 | 0 | 对 mcm51-c 通用规则也无触发 |
| runs/mcm51-b-brief-v1 | 3 / 3 | 0 | 同上 |
| mathorcup16-c/brief.json（v1 无规则） | — | 恒通过 | **v1 向后兼容确认** |

- 观察结论：现有 7 条规则在 16+ 个真实代码文件上**零命中、零误报**——pattern 偏保守（仅明显违规触发）；warn 观察期数据支持「规则可保持或收紧」。⚠️ 局限：样本均为已通过评审的旧 run，未覆盖「真实违规」样本——hard 升级决策仍需 1 个带违规的演练样本。

## 3. 覆盖矩阵对照（C 类验证点覆盖状态）

| 验证点 | 覆盖方式 | 状态 |
|---|---|---|
| 3.1 切片注入 | test_brief golden + evaluation/paper_critic scoring 测试（D-022 已有） | ✅ 已覆盖 |
| 3.2 review 接管 | test_review_reroutes_stopped_run_to_human_review 等 3 个（D-023 已有） | ✅ 已覆盖 |
| 3.3 restart 接续 | test_restart 6 个（含 3 拒绝路径，D-023 已有） | ✅ 已覆盖 |
| 3.4 停机提示面 | 本轮补 8 个 reason 分支测试 | ✅ 已覆盖 |
| 3.5 红线观察 | 本轮离线观察（4 run / 16+ 文件） | ✅ 机制面已覆盖（真实验仍待 1 次带违规演练） |
| LLM 行为面（prompt 注入后模型是否正确使用） | 不可单测 | ⏳ 仍待 1 次真实 run（预算敏感，暂缓） |
