<!-- doc: type=state status=active updated=2026-08-31 -->
# 01-状态（摘要 · 事实以 00 §三 为准）

- **定位**：上游 Beacon 的 fork。默认路径 = 人 / 外部 agent 走 S0–S8；`start`/`supervise` 是 S9 可选工具（D-002 / D-005）。论文本体仍由 Beacon 产（D-020）。
- **操作面已成形**：P0–P6 完成。`problem` / `brief` / `reference`（含 `expand`）/ `review-check` / `accept`。无 T-19 / inject 时 coder LLM 默认关（D-024）。
- **8-29 硬信号**：D-022 brief 切片 + 红线；D-023 门禁一次停；D-025 inject；D-026 consistency 接续；ACT-03/04 关账。
- **题目**：mcm51-c = S0–S8 范本；mathorcup16-c = S0–S8 实验题（领域知识不补）；mcm51-b = 赛后校准第一例（H-04 on=37280）；mcm51-a 不再做题。
- **文档**：治理完成；**R2 / D-027** 核心入口已按 fork 现状重写（2026-08-31）。
- **下一步**：按需开题（P-06）；改体系从图景 03 抽；S0–S3 走查仍待做。ACT-08 注入 `background_knowledge` 仍不做。

→ 事实源：[00-体系现状与原则](../00-体系现状与原则.md) §三
