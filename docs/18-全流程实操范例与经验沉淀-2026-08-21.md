# 全流程实操范例与经验沉淀（2026-08-21 · ⚠️ 特别重要）

> **定位**：**可迁移的未来使用范例**。完整记录 mcm51-b 从「题目探索 → brief 生产 → 求解实现 → 论文构建」的
> 首次全流程实操：轨 A（Beacon 流水线）与轨 B（skill 化多 Agent）双轨并行、流水线 5 次故障与修复、
> 参考实现两处隐蔽 bug 与 3 轮数据重算、多 Agent 交叉审查的价值实证。
> **未来任何新题开工前，先读本文件「§六 失败模式清单」与「§七 可迁移流程」；新经验持续回填。**
> 状态：进行中（轨 A 待冻结版重跑；轨 B 评审待完成）——记录截止 2026-08-21 晚。

- 关联：[brief-playbook.md](brief-playbook.md)（方向生产协议）、[实现计划-8-20](实现计划-8-20/README.md)（执行层）、
  [07-待决策与超范围](实现计划-8-20/07-待决策与超范围.md)（决策点 19/19a：双入口与平台-vs-skill 双轨对比）、
  [16-中长远路线图](16-中长远路线图.md)（C 路线：收缩薄产品）。

---

## 一、背景与双轨设计

- **战略质疑（2026-08-21）**：方向生产（playbook）与编码（参考实现）都已外置后，「体系剩下的只是论文一致性协调，
  是否完全没必要通过体系？」→ 立为决策点（07 19a），用**同输入双轨对比**（质量/成本/可靠性）回答，不靠观点。
- **轨 A**：Beacon 流水线（supervise → brief → analyst…finalizer），测「体系原样能力」；
- **轨 B**：skill 化多 Agent（数据 → 写作 → 评审，独立文件夹产物），测「agent+prompt 直接干活」。
- 实验题：mcm51-b（2026 五一赛 B 题 多工序协同作业：调度/排程类，非物流非力学——体系首次面对的类型）。

## 二、阶段 0：方向生产（外部，playbook 实操）

> 全程在体系外完成（人 + 外部强模型 + 联网），体系零参与。产物形态逐轮演进，三份原始输出归档于
> `实现计划-8-20/实际操作记录/`。

| 步骤 | 提示词 | 产出 | 形态特征 |
|---|---|---|---|
| 1 开放探索 | playbook §2.1「提示词 0」 | 第一论.md：**3364 行 / 75 个候选点** | 思考路径，未收敛，大量互不兼容方向，不可直接使用 |
| 2 探索消化 | playbook §2.5「提示词 0.5」（**同一会话追问**） | 第二轮.md：认知地图 v1（10 小节、🔴🟡⚪ 优先级、不兼容标注、5–8 个论文探索点判定） | 收敛、可读、可用 |
| 3 逐问清点 | playbook §4.1「提示词 A」 | 第三轮.md：**52 条隐藏约束/歧义裁决** + 核心口径「设备完成≠工序完成」+ 附件消耗清单 + 反向审查 + 最终锁定口径 | 建模前口径全部钉死 |

**关键经验**：
1. **探索产出是思考路径，必须第二轮消化**（playbook §2.5 由此诞生）——第一轮再强也不会直接给认知地图；
2. 消化必须在**同一会话**追问（上下文自带思考流），不要新开会话重来；
3. 52 条裁决按 playbook §5.1 压缩规则映射进 brief（assumptions/red_lines/data_notes），**探索结论不进体系**，只进 brief 与 exploration.md 档案；
4. 防泄漏纪律贯穿：禁止检索官方评分/参考答案（已公开题只作机制调试）。

## 三、阶段 1：资产与工具链（机械层，2026-08-21 当天开发）

- `problem import/show`、`run --dry-run`（feasibility.blockers/附件存在/out 冲突一次全报，零烧钱）、
  `brief init/check`、`supervise --force`（CLI 增补见实现计划-8-20 00 设计 O1–O4）。
- 资产目录：`problems/mcm51-b/{source(归档+manifest.json 哈希), problem.md, problem.json(spec v2), brief.json, exploration.md}`。
- **踩坑记录**：
  1. `data_files[].path` 是相对 `data_dir` 的——写 `source/B-附件.xlsx` 会重复拼接成 `source/source/...`；
  2. spec title 用复合写法（含 ASCII 竞赛名）避免 B04 题号匹配误报（中文题名提取不出 `mcm51` token）；
  3. `source/` 下英文版附件（B-attachment.xlsx）与中文版并存时，**按工作表名识别**（`工序流程表`），glob 第一个会拿错文件。

## 四、阶段 2：轨 A（流水线）实操——5 次运行的故障谱

| # | 运行 | 停点/结果 | 根因 | 修复 | 成本 |
|---|---|---|---|---|---|
| 1 | supervise（初始 brief 40 条） | 一致性门禁 0/10 停机 | **total_cost 守卫误杀**：非物流题输出校验把通用成本名当物流指标整份拒绝 → 无主证据 | `_GREEN_METRIC_NAMES` 移除 total_cost（**校验要落语义，不能只看名字**） | 356k tokens |
| 2 | restart --from coder | 再次停机 | 生成代码结构性错误：C 工序名短名/全名混用（执行即挂）+ Q1 调度全车间、单设备替代双设备、经班组驻地转运、贪心替代 CP-SAT | 决策 A：brief 加实现级约束（formula-solver/cloop-impl + 4 红线 + data-cnames） | ~15 万追加 |
| 3 | 全新 run（A 路径） | 再次停机 | **效率字符串解析失败**（附件格式 `精密灌装机200m³/h` 设备名与数值间无空格）——A 路径失败 | **决策转 B**：人工供参考实现 reference_solver.py（纯 Python，30 分钟写完并本地验证） | ~30–40 万 |
| 4 | 全新 run（B 初版） | 一致性 0/10 | **接近成功**：coder 正确 exec 参考实现、PNG 全出；但包装代码用**臆造 regex**（makespan/total_cost/budget_utilization 连排）解析 RESULT 失败 | 参考实现 RESULT 字段重排 + brief「exec 后**原样透传**、禁重解析、禁自绘」+ rl-result-format 红线 | ~15–25 万 |
| 5 | 全新 run（B 修复版） | **跑到 writer 阶段（~90%）**，但因参考实现 C 循环链 bug 数值失效 → **停跑**，等冻结版重跑 | 参考实现两处 C 循环链 bug（见 §五）在轨 A 运行期间才被发现 | 冻结参考实现后重跑（待用户确认） | ~40–50 万 |
| 6 | 全新 run（冻结版，05:31） | **全流程首次跑通**：一致性 score=9 通过 → sensitivity/figure/writer/evaluation → human_review 暂停（82 调用 / 536.2k tokens）；evaluation 综评 7.0（5 问题）；finalizer 若批准将以 degraded 收口（对照方案 0/论文 7/10/结果正确性 6） | 机制成功但内容三缺陷：① 模型章声称 CP-SAT/MILP 精确求解 vs 实际 exec 参考求解器（启发式）——违反 rl-greedy 红线；② 敏感性基准 124943.7 与主方案/Q3 均不符（**脚本根因已查证：budget 参数未用、未取整、C 展开错序**）；③ 40.9%/41.0% 无依据且不一致 | 已批准前停在 human_review（批准与否另案）；A/B 互补评审报告见 runs/mcm51-b-brief-v1/A-B互补评审报告.md | 536.2k tokens |

**机制级教训**：
1. **门禁停机（无主证据）→ `recover` 空转**（checkpoint `next=()`，B05 语义）——修根因后只能全新 run；
2. **每次 brief 变更 → restart 输入不变性校验拒绝** → 全新 run，单次成本 35–50 万 tokens——小修复的大代价，
   这正是恢复性机制（计划 11：pause/restart 人工放行）与「人工放行重试 coder」的价值来源；
3. **coder 对新题型的不可靠是结构性的**（4 次失败全部是生成代码 bug：守卫、键名、解析、契约）——
   **参考实现（人工供给）是可靠的降级路径**，且与「方向生产外置」是同一哲学：最贵的智能放在前端；
4. 修 prompt 能 recover、修 brief 只能重跑——输入不变性校验本身是对的，但要提前算进成本。

## 五、阶段 3：轨 B（skill 化多 Agent）实操

**产物独立文件夹**：`runs/mcm51-b-skill-v1/`（与轨 A 的 `runs/mcm51-b-brief-v1/` 平行对比）。

### 5.1 分工设计（子 Agent 三分离）

| Agent | 职责 | 关键产物 |
|---|---|---|
| 数据 | 跑参考求解器 + 敏感性（资源/速度/预算曲线/CPM 下界）+ 证据清单 | `data/solver_full_output.txt`、`data/sensitivity.json`、`data/evidence.md`（每个数字→来源文件+行号） |
| 写作 | 按 brief 八条 required_discussions + 章节白名单写论文 | `paper.md`（表 1–5 从真实输出逐行摘录，自查回源） |
| 评审（待启动） | 参考实现 vs brief 口径系统审计 + 论文数值回源/一致性/诚实性 | 核查报告 |

### 5.2 多 Agent 交叉审查的价值（本轮最大收获）

**写作子 Agent 独立抓出参考实现两处隐蔽 bug**（探索层早就写对、实现层漏掉的口径错误）：

1. **C 循环链只展开 C3**：题面备注「C3–C5 完成 1 遍后需再重复 2 遍」= 三种工序整体三轮（9 活动），
   实现只把 C3 展开三轮、C4/C5 各一次 → 修复为 C1→C2→(C3_1,C4_1,C5_1)→…→(C3_3,C4_3,C5_3) 共 11 道活动；
2. **C1/C2 键名不匹配排到链尾**：排序键用短名 `"C1"` 查活动全名 `"C1.旧涂层剥离"` → 回退 99 → 链序变
   C3_1→…→C5_3→C1→C2 → 修复为前缀正则键 → 链序正确（Q3：144859→127085，-17774s）。

**结论：独立 agent 交叉检查能发现同源自审发现不了的口径错误（H04 相关判断者的正面解法）——
参考实现交付前必须做「实现 vs 口径」系统审计（评审 Agent 的固定职责）。**

### 5.3 数值演化（三轮重算，最终口径）

| 问 | 初版（C3 只展开） | 修复 1（整体三轮但链序错） | **最终（链序修正）** |
|---|---|---|---|
| Q1 | 41600s | 41600s | **41600s**（精确最优，CPM 差距 0%） |
| Q2 | 147143s | 215303s | **215303s**（差距 74.2%，单班组容量不足） |
| Q3 | 135068s | 144859s | **127085s**（差距 2.81%） |
| Q4 | 116209@75000 | 123844@155000 | **123844@155000**（多功能机×1+抛光机×1 班组1；距 CPM 下界 0.19%） |

**敏感性发现（可迁移的建模素材）**：
- **瓶颈判断**：自动传感多功能机 +1 → −1.68%（唯一有效单台增量）；高速抛光机单独 +1 无效；
- **互补瓶颈效应**：抛光机作为第二台购置（多功能机之后）再省 ~1100s——**单台边际分析会低估组合购置价值**，
  预算-工期曲线因此呈**双拐点**（100000/200000）而非单拐点；
- **速度敏感性极弱**（1.5→3.0 m/s 仅 −0.38%，边际递减）——瓶颈是加工容量而非转运；
- **CPM 无容量下界对照**：关键链 C=123614（取代 D）；Q4 距下界仅 230s（0.19%）——启发式在双班组+购置下几乎达无容量下界。

## 六、失败模式与反模式清单（⚠️ 可迁移，最重要）

1. **校验只看名字不看语义**：`total_cost` 通用成本名被物流指标黑名单误杀（守卫误伤类问题的通用教训：
   确定性校验要落在语义/组合信号上——M3 红线机器化设计时沿用）；
2. **键名混用**：全名 vs 短名、别名、大小写——生成代码与数据字典必须同一套键（C1 vs C1.旧涂层剥离）；
3. **隐藏结构未在实现层验证**：C 循环链——探索层（第三轮）写对了「9 活动」，实现层漏了；
   凡是 brief/认知地图标注 🔴 的结构性口径，参考实现交付前逐条核对；
4. **生成代码臆造契约**：包装代码自己发明 RESULT 格式 → **契约显式化**（参考实现给出逐字格式）+
   「原样透传、禁重解析、禁重绘」；
5. **启发式冒充精确主方案**：必须诚实声明 + CPM 下界对照（差距% 是论文的诚实性证据，不是减分项）；
6. **门禁停机 recover 空转**：无主证据停机 = 修根因重跑（或计划 11 的 restart 人工放行），不要 recover；
7. **输入变更成本意识**：改 brief 一次 = 全新 run 一次（35–50 万 tokens）——把修复合并批次再跑；
8. **单 Agent 自审盲区**：数据/写作/评审三分离，独立 agent 交叉抓口径错误（本轮两处 bug 皆由此发现）；
9. **附件多版本并存**：中文/英文副本按内容识别，不按文件名/glob 顺序；
10. **运行中修改参考实现**：轨 A 的 coder 运行时才读文件——修复会半路生效造成「数字来源混乱」；
    参考实现必须**冻结后**再启动任何消耗性运行（冻结=哈希登记 problem.json source_files）。

## 七、可迁移流程（未来新题 runbook）

```
0. 读本文件 §六 失败模式清单 + brief-playbook 核心原则
1. 方向生产（外部）：提示词 0 探索 → 0.5 消化（同会话）→ A 逐问清点 → B 多模型对撞
   → 认知地图 v1（exploration.md）+ brief.json（brief check [OK]，无参考值泄漏）
2. 资产导入（机械）：problem import → 补全 problem.json（title 复合写法/五字段/feasibility）
   → run --dry-run --force 预检
3. 求解实现决策：
   - 简单/标准题型 → 直接轨 A（让 coder 试，失败再降级）
   - 复杂/多隐藏结构题 → 直接参考实现（人/agent 供给）：实现 vs brief 口径逐条审计
     （活动数/链序/双设备/运输/范围/RESULT 契约）→ 冻结（哈希登记）→ 才允许消耗性运行
4. 论文生产双轨：
   - 轨 A：supervise --force（冻结参考实现后）
   - 轨 B：数据子 Agent（solver/敏感性/CPM/evidence）→ 写作子 Agent（八条 discussion/表 1–5 真实数据/零编造）
     → 评审子 Agent（实现审计 + 论文回源/一致性/诚实性）
5. 对比与决策：质量/成本/可靠性 → 07 19a
6. 沉淀：新失败模式回填本文件 §六；数值/流程回填实现计划与 README
```

## 八、成本与时间账（粗略，精确值见各 run 的 cost_summary/progress.jsonl）

- **轨 A**：run1=356k tokens（实证）；每次全新 run 估计 35–50 万 tokens；5 次运行 + 多次修复合计
  **约 120–160 万 tokens 无有效论文产出**（最后一次跑到 90% 因数值失效停跑）；人工排障约 2–3 小时；
- **轨 B**：**零 LLM 调用**（纯 Python 数据 + 子 Agent 写文档/论文）；agent 执行时间约 1–2 小时；
- **初步倾向（待最终对比）**：在「编码不可靠」的题型上，轨 B 成本与可靠性显著占优；
  轨 A 的剩余价值 = 证据链/门禁/格式纪律（可被 skill 化部分吸收）——这正是 07 19a 要测的。

## 九、未决与后续

1. 轨 A 冻结版重跑（用户确认后；参考实现**已冻结**：哈希 `504ecc0a8fa5c006`，评审 2026-08-21 通过——9 项口径实测全过/12 条红线零违反/确定性复现/无硬编码；**任何修改须重新登记并重估数值**）；
2. **轨 B 评审已完成（2026-08-21）**：参考实现「可冻结」、论文「可交付」——数值 100% 回源（表 1-5 共 133 行逐行吻合 + 独立抽查 67/67 + 全部 41 个 ≥4 位数字回源）、八条 discussion 全覆盖、一致性与诚实性合格、文献合规；P1 措辞（§8.3/§7.3「最优购置方案」→「本文所得购置方案」）已修复；P2（补预算-工期曲线图与设备移动图）与 P3（Q2 甘特图设备纵轴）为图相关建议，**按用户指示暂缓**（与轨 B 图片可读性问题一并留待定稿处理）；
3. 双轨最终对比 → 07 19a Go/No-Go；
4. CP-SAT 精确化（Q2–Q4 非最优是当前最大质量短板，列为方法能力库升级项）；
5. 恢复性机制（计划 11：pause/restart）真跑验证——本轮 5 次重跑直接由「门禁停机不可续」推动；
6. 平台-vs-skill 的结论可能导向 16 C 路线（收缩薄产品：证据校验库 + skill 化流程）。
7. **轨 B 甘特图可读性（用户观察 2026-08-21，暂不改轨 B 现有步骤）**：参考实现 draw_gantt 样式缺陷——
   颜色偏深（#4C72B0）、每根 bar 标注设备 id、27 活动挤一张图，几乎不可读。留待轨 B 定稿（真实投入生产）时统一处理：
   浅色系、稀疏标注（仅关键 bar 标设备）、分车间/分问分图、字号规范；并对比轨 A figure 管线（figure_critic 有图质量审查）
   的可借鉴点。

## 十、速查

- **命令**（E:\git_clone\Beacon 下，`.venv\Scripts\python.exe`）：
  `problem import/show`、`run --dry-run [--force]`、`brief init/check`、`supervise --force`、`restart --from coder --reason ...`、`watch`、`status`
- **关键文件**：
  `problems/mcm51-b/{problem.json, brief.json(50 条), source/reference_solver.py, source/B-附件.xlsx}`
  `runs/mcm51-b-brief-v1/`（轨 A）、`runs/mcm51-b-skill-v1/`（轨 B：paper.md + data/{solver_full_output.txt, sensitivity.json, evidence.md} + figures/）
  `实现计划-8-20/实际操作记录/{第一论,第二轮,第三轮}.md`
- **轨 B 子 Agent 提示词要点**：数据 Agent（路径/复现命令/敏感性清单/evidence 格式）；写作 Agent
  （必读文件清单/章节白名单/八条 discussion 逐条落点/零编造自查回源/诚实声明/文献真实性规则）；
  评审 Agent（口径逐条核对表 + 回源抽查 + 一致性 + 诚实性）。
- **轨 B 评审 Agent 口径核查必答项（C3 注入，2026-08-26）**：评审 Agent 输出必须逐项覆盖——
  ① 假设逐条核查（"题面明确要求"类声明须有题面原文依据；同假设内"明确要求"与"未要求"并存视为矛盾，
  跑 `scripts/check_assumption_claims.py`）；② 大差距根因（任何下界/对照差距 >15% 必须给机制性解释，
  禁止免责声明，跑 `scripts/check_gap_trigger.py`）；③ 数字溯源与敏感性口径（正文数字与脚本输出不符视为
  编造；敏感性档位必须出自实跑，跑 `scripts/check_paper_numbers.py --strict`）；④ 得分点覆盖
  （有细则/得分点清单逐条核对，无则检查题面指定交付物齐备性，见 `8-26-F类-得分点清单模板.md`）。
  轨 A 的 evaluation/paper_critic prompt 已同步注入（src/math_agent/prompts/，2026-08-26）。
- **轨 B 求解脚本产出规范（五条，B 类轻量版，2026-08-26 用户确认）**——数据 Agent 的求解脚本必须满足，
  评审 Agent 逐条核验：
  1. **口径参数化**：口径相关假设（允许同类分摊、取整规则、进场运输、不可抢占、双设备异步等）必须做成
     脚本**顶层参数/常量**，禁止写死在求解逻辑里；参数默认值与 brief 裁决一致并注明出处
     （mcm51-b 教训：假设 9 写死在构造式启发式里，纠正成本 = 整轮重算；参数化后 = 改一个开关）。
  2. **分歧双跑**：brief/方向生产裁决过并保留备选口径的分歧点，必须产出**两组数字 + 对比表**
     （主口径 vs 备选口径，含差值与百分比），作为 evidence 的一部分——备选口径下结果会差多少，
     必须成为可引用的数据，而不是赛后才发现的盲区。
  3. **诊断明细**：输出必须含：每工序耗时明细（可回溯到表 1-5 每一行）、设备利用率、瓶颈识别
     （最长链/占用率最高设备）；RESULT 行格式统一（`RESULT: baseline=ours makespan=…` 全关键值可 grep）。
  4. **evidence 格式**：每个关键数字 → 来源文件+行号（evidence.md）；输出文件与论文数字一一对应。
  5. **哈希登记**：脚本定稿后登记 sha256（problem.json source_files 或 run 产物），作为可复算锚点；
     修改脚本须重登记并重估全部数字（轨 B 数字 100% 回源的前提）。
- **双轨实验约定（G 类，2026-08-26）**：① 至少一轨带**独立口径复核**——第二读者（不同模型/人）重新读题
  产出竞争口径集，与主口径对比后再定稿；② 互补评审结论必须声明**同源盲区**（双轨共享 brief 与求解口径时，
  只能比较执行质量，不能作为建模正确性证据；mcm51-b A/B 互补评审未触达假设 9 即同源所致）。

## 更新记录

- 2026-08-21 建立（首次全流程实操范例：探索→brief→参考实现→双轨论文；5 次故障谱；C 循环链两 bug 与三轮重算；失败模式清单；未来 runbook）。
