# MCM-51 迭代交接文档（r11 之后、下次迭代前必读）

- 建立日期：2026-08-18
- 适用范围：MCM-51 煤矿巷道锚杆支护题（`runs/51mcm-a-sens-scan-r11` 之后的所有后续迭代）
- 定位：**跨迭代交接备忘录**。论文产物之外，哪些信息必须保存与传递、哪些已确认可靠、哪些必须重点解决、哪些明确留待后续流程。
- 事实基线：`git log` 至 `4b3ddb4`；全量测试 `708 passed / 4 skipped`（2026-08-18）

---

## 一、已确认无问题的部分（后续迭代可放心依赖）

以下内容**已由代码、测试或运行产物验证**，后续迭代不需要重新怀疑：

| 类别 | 内容 | 证据 |
|---|---|---|
| 主证据数值 | K=0.176681、整体 R²=0.958852、分直径 K（18/20/22mm = 0.162003/0.183069/0.189868，R²≥0.96）、T_max,A=600.71、T_max,B=430.56、T_opt,A=480.57、T_opt,B=344.45、安全裕度 0.2000、P_max,A=170000 N、P_max,B=110769.23 N | r11 一致性门禁 9/10 通过（`steps/0062_model_code_consistency`），数值全部来自真实代码执行 stdout，非编造 |
| 问题1.1 回归 | 表1 数据按直径分组线性回归（整体+分直径），契约与结果一致 | 论文 1.1 节 + 代码输出 Q1.1 行 |
| 问题2.1/2.2 | 三失效模式（屈服/粘结/压陷）min 模型、钢带判据（P_δ<P_y 且 P_δ<P_b）、工况A 无需钢带、工况B 必须加钢带 | 论文 2.1/2.2 节 + Q2.2 代码输出 |
| 问题3.1/3.2 | 偏心等效应力模型、e=0 退化一致性（修正模型退化为原模型） | 论文 3 节 + Q3.2 代码输出 |
| 机械层修复链 | P01–P16 全部落地：门禁死锁、7 分死区、证据白名单、`\n` 自动修复（L2）、mtime 产物检测、NaN/列名提示、LIMITATION 协议等 | [issue-report 第二节](2026-08-18-mcm51-issue-report.md)（P01–P16）+ 对应 commit |
| 图收集机制 | figure_pipeline 已修复：非物流题接受 supporting 图 + R²/T_max/T_opt/安全裕度偏差 >20% 或灾难值拒绝（`009053b`） | 4 个新测试 + 全量 708 passed |
| 人工接管通道 | `math-agent review`：paper_critic 未过但论文完整时可推进到 human_review（`f703e8a`），r11 已实测走通 | r11 当前 `next_node=human_review` |
| 论文诚实化表述 | 三处"利用表2/表3 确定 600.71"已改为诚实声明（见第三节的表述修正记录） | r11 checkpoint paper 字段 + `paper_preview_r11.md` |

> **重要提醒**：r11 论文数值（600.71 等）是"真实执行的代码输出"，**不等于物理正确**——表2/表3 实测预紧力矩范围为 25~375 N·m，600.71 超出该范围（见第三节 P-T 缺口）。"无问题"仅指**证据链真实、机制可靠**，不指模型结论已达标。

---

## 二、需要着重做工作和解决的部分（按优先级）

### 2.1 机制缺陷（代码层，未修复）

| 编号 | 问题 | 影响 | 修复方向（已定位） | 关联 |
|---|---|---|---|---|
| **P17** | 一致性评审"自认契约未实现却放行"：r11 评审 issues 自认"未实现分段线性回归求 T_crit、直接取 T_max_A"，仍给 9/10 | 问题1.2 表2/3 利用从未实现就放行 → paper_critic 两次扣分 | backstop 关键词扩展：issue 同时含"未实现/未做/直接取"+"pass_criteria/题目要求/验证" → 强制不通过；"略高于合理范围"也纳入 | issue-report P17 |
| **P18** | sensitivity 口径错配：代码自写 `E=f*1e9` 映射，主方案无 f↔E 定义 → 中心对齐校验 3 次失败 → 敏感性无数值 | 论文敏感性只能定性 → paper_critic 扣分 | sensitivity 计划参数必须存在于主方案代码（f 不在则跳过中心对齐或改扫 E）；code prompt 注入主方案参数清单（E=15GPa、K=0.176681、η=0.8） | issue-report P18 |
| **P20** | writer 修复轮对"证据缺失"问题无效且新增无证据断言（"数值验证表明 e=0 退化一致"） | 修复轮烧预算，7→6 反而下降 | writer 修复 prompt：证据列表外的"验证表明/结果表明"一律禁止写入 | issue-report P20 |
| **O06** | blueprint pass_criteria 数值区间（100-500 N·m）未机器可读提取 | 600.71 超范围靠评审"同数量级"放行 | 解析 pass_criteria 区间 + 确定性拦截 | issue-report O06 |

### 2.2 论文实质缺口（内容层，下一轮 run 必须改进）

1. **问题1.2 临界预紧力矩**（最高优先）：题目要求用表2/表3 实测数据（测点预紧力，T 范围 25~375 N·m）确定临界预紧力矩；r11 实际用工况A 参数（表4）算得 600.71，**超出实测范围**。下一轮必须在 coder 阶段真正实现表2/表3 的分段线性拟合/屈服点识别（配合 P17 修复逼评审拒绝低质量实现）。
2. **问题4 定量化**：f 扫描需给出具体数值与主控失效模式切换阈值（当前仅定性"低 f 压陷控制、高 f 屈服控制"）。前置依赖：P18（f↔E 映射必须先定义）。
3. **敏感性定量扫描**：当前"未执行定量参数扫描"（诚实声明合法，但国赛要求定量）；修复 P18 后应产出 K/η/E 扫描数值+图。
4. **图数量**：r11 仅 1 张图；figure_pipeline 机制已修好，下一轮 supporting 图会自动进入（前提：支持图数值与主方案一致，否则被一致性校验正确拒绝）。

### 2.3 已人工修正但未机制化的内容

- r11 论文三处"利用表2/表3 确定 600.71"已改为诚实表述（摘要→"结合工况A参数（表4）计算"；1.2 节→"600.71 超出实测范围 25~375，口径不一致，局限已说明"；结论→"未将表2/3 用于临界点识别，与题目要求存在差距"）。
- **注意**：这是人工 patch（`update_state` 写回 checkpoint），**机制上未固化**——下一轮 run 的 writer 仍可能写出同类不实表述。P17/P20 修复是治本。

---

## 三、明确留待后续流程解决的内容

| 事项 | 状态 | 说明 |
|---|---|---|
| r11 论文最终产出（LaTeX/PDF） | **待人工批准** | r11 停在 human_review。批准：`uv run math-agent resume --out E:\git_clone\Beacon\runs\51mcm-a-sens-scan-r11 --thread 51mcm-a-sens-scan-r11 --approve`（产出 degraded 状态，质量警告如实记录）；拒绝：`--no-approve`。批准前可先审阅 `runs/51mcm-a-sens-scan-r11/paper_preview_r11.md` |
| 手动补图 | **用户决策：不强行补** | 磁盘 supporting/sensitivity 图数值口径与主方案不一致（fig_2 e=0 为 347 vs 主方案 600.71；fig_1 灾难值 0.08），强行补入制造矛盾。图片数量作为后续重点补强工作（figure_pipeline 机制已修好） |
| 图补强的具体策略 | 待规划 | 后续迭代目标：论文 ≥4 张图（回归图+分段拟合+偏心敏感性+参数扫描）；图必须与主方案数值同口径 |
| 模型层剩余差距 | 观察中 | 螺纹强度量纲、P-T 转换关系、钢带判据、f↔E 经验关系（issue-report O03） |
| resume-with-reset 完整版 | **未实现** | 通用"重置预算续跑"仍未实现；已有窄化版（review 人工接管）。完整版待 r12 收敛情况评估（issue-report O10） |

---

## 四、关联文档与产物索引

| 内容 | 位置 |
|---|---|
| 问题清单与已落地解决方案（P01–P20、O01–O10） | [`docs/2026-08-18-mcm51-issue-report.md`](2026-08-18-mcm51-issue-report.md) |
| r11 运行证据（steps/insights/gate_diagnostics） | `runs/51mcm-a-sens-scan-r11/`（`steps/` 313 个 checkpoint 步骤、`insights/` 10 个 insight 文件、`checkpoints.sqlite`） |
| r11 论文完整预览（诚实化后，22095 字符） | `runs/51mcm-a-sens-scan-r11/paper_preview_r11.md` |
| r11 图像产物（7 张有效 png，未全部采用） | `runs/51mcm-a-sens-scan-r11/fig_*/`、`sensitivity/attempt_2/` |
| 附件数据（表1–表4） | `runs/ui-server/ui-1786947427744-0868bcc4/data/A-附件.xlsx`（表2/表3 实测 T 范围 25~375 N·m） |
| 门禁与评审阈值 | `src/math_agent/config.py`（MIN_MODEL_CODE_SCORE=8、MIN_PAPER_CRITIC_SCORE=9、MAX_WRITER_ITERATIONS=3）；r10/r11 用环境变量 `MATH_AGENT_MAX_CODE_VERIFY_ITERATIONS=5` |
| 本迭代 commit 链 | `c855485`（mtime 产物检测）→ `d36b414`（评审证据）→ `cc1bfe1`（O10 文档）→ `f703e8a`（review 接管）→ `009053b`（figure_pipeline 修复）→ `4b3ddb4`（报告更新） |

---

## 五、下一轮启动前检查清单

- [ ] 决定 r11 命运：`resume --approve` 产出 / `--no-approve` 放弃（不阻塞下一轮，但产物需留档）
- [ ] （建议）落地 P17 backstop 扩展（最小改动，防"自认缺失仍放行"复发）
- [ ] （建议）落地 P18 sensitivity 口径（f↔E 映射或改扫 E，敏感性定量化的前置）
- [ ] （建议）落地 P20 writer 修复轮禁止无证据断言
- [ ] 启动新 run 时沿用 `MATH_AGENT_MAX_CODE_VERIFY_ITERATIONS=5`（低分预算 5 轮）
- [ ] 新 run 关注点：问题1.2 是否真正用表2/3（看 coder 代码+stdout，不看论文表述）；sensitivity 是否产出定量扫描；论文图数量是否 ≥4
