---
type: log
status: active
updated: 2026-09-05
---

# 2026-09-05 · 注入式全图正式流落地（A–C + 定稿）

## 完成

- `math-agent plan build|check`：coverage 骨架带 question_ids/equation_ids 锚
- `run --plan`：播种 analyst 产出位；`plan_injected` 使 modeler 短路；plan_sha256 进 manifest
- 敏感性：`--plan` 路径缺 inject 为 `sensitivity_phase=stop`（旧路径仍 done 滑走）
- 交棒：`edit_code` 含 `reference add --force`；`edit_plan` 要求新开 run
- cumcm23-c `plan.json`：`plan check` 零缺口
- 定稿 00/10/13/13附/14/15 与根 README / ops_help 改为正式入口 `--plan`

## 自行拍板（非方向）

- 不做 plan.sensitivity_runs 短路（按拍板：子图仍 LLM 出计划）
- C4：冻结哈希失败时 WARN 提示 add --force，不另做 mtime 闸
- plan.sensitivity_runs 不写入图状态，避免与图内新跑混台账

## 未做

cumcm23-c 全图 LLM 真跑（D4）。

## D4 前置（inject 脚本）

- 新增 `problems/cumcm23-c/source/inject/sensitivity.py`：按研究网格重跑成本冲击 / 加成分位 / 损耗率缩放，打印 `RESULT: parameter=...`；中心点对齐 `_entry` 的 `q2_week_profit`（单测验证）。
- `wrap_inject_sensitivity`：`exec` 注入 `data_dir`，避开 `from __future__` 插队。
- `plan.json` 补 `sensitivity_relevant` 三条，把扫参参数名钉成 `成本冲击 (cost_shock)` 等，方便 LLM plan 与 inject 对上。
- 不做 `plan.sensitivity_runs` 短路。

## 定稿加厚（真跑教训写进 00）

- 重写 `全流程分析/全流程定稿/00-正式闭环.md`：brief/plan 分工、第四幕命令序、图内节点、`edit_code` vs `edit_plan`、数字口径、首跑坑（血缘/PNG/inject/blueprint 空字段）。
- 第五节工程入口改为 `run --plan`，不再把 `--from writer` 写成产品命令。
- 对齐 11/12/13/13附/14/15/16、定稿 README、根 README、ops_help；13附去掉 `研究/beacon_ref` 误路径。

## D4 续 + 经验夹（2026-09-05 下午）

- r2：paper_critic 9 过闸；latex 出 PDF；inalizer degraded（页数 18、对照 0、Overfull 4）。
- 过程经验写入 `全流程分析/图内真跑/`（怎么记 / 案例 / 操作坑 / 系统设计债）。用法仍只看定稿 00。
- `review-check --strict` 与人验收未做。

- 定稿 00§8 / 16 / 规划 D4 消化 `degraded` 口径；账本 `全流程分析/图内真跑/落地修改-2026-09-05.md`。

## D4.3 与讲解对照（2026-09-05 傍晚）

- 关键 RESULT 六键均在 r2 `paper.md`；`review-check --strict` 仍 exit 1（tokenizer / L4 缺「主口径取舍」/ brief caption 未逐字）。不把这当成注入流失败。
- 讲解 3.9 原稿「图外再跑一遍」已改为：写后对账扫已有 evidence，不重跑求解。
- 定稿 00 §7 / 15 / 16 写清怎么读报告；敏感性节模板要求原词「主口径取舍」（111 pytest：三刀 + plan + ops_help）。

## 学-32 对账/对照口径（2026-09-05 傍晚）

- `review-check --strict` 改为 `--key-results`：只核关键 RESULT。r2 对该命令 exit 0。
- L4 G1 与 brief caption 逐字改为 WARN。
- finalizer 认同 stdout 内结构化 `RESULT: baseline=<名>` 对照（须与 ours 指标可比且数值不同）。本题 r2 对照仍在 Q 行，质量门仍为 0。
- 页数 / table 时序未改。pytest：check_paper_numbers / l4 / ops_review / finalizer 相关 109 绿。

## 学-33 锁正式流程（2026-09-05 晚）

- 定稿 00 写明：锁入口与交棒，不锁交卷体面；r2 是流程样板不是可交样板。
- 规划文迁 `全流程分析/archive/规划-注入式全图正式流.md`（status=superseded）。

## 对照门变绿（cumcm23-c r3，2026-09-05 晚）

- `_entry` 补 `RESULT: baseline=WD90/YOY`（每行 ≥3 指标，与 ours 共享 `blend_mae`）。
- r2 已终态不能 restart；开 r3。首枪因单指标 RESULT 与 `fig_*` PNG 差集空失败；清目录后过一致性。
- paper_critic 曾因引图后 `markup_p/loss_scale` 泄漏停；writer 引图后再洗一次（产品补丁）。
- r3 `completion.json`：`degraded`，警告仅页数 19 + Overfull 10；**对照门已无**。
- `review-check --strict`（evidence=reference-baseline2）exit 0。
- `accept` 已写 `problems/cumcm23-c/acceptance.json`（assumed；未 push）。

## 产品补丁：min_metrics 与冻结 PNG（2026-09-05 夜）

- `validate_numeric_results`：`min_metrics` 只约束 `ours`（或 baseline 的 expected id）。
- 冻结 figure：`artifact_paths` 空但工作目录有 `*.png` 时回认（`copy2` 同 mtime）。
