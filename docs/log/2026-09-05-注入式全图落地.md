---
type: log
status: active
updated: 2026-09-05
---

# 2026-09-05 · 注入式全图正式流落地（A–C + 定稿）

## 完成

- `math-agent plan build|check`：coverage 骨架带 question_ids/equation_ids 锚
- `run --plan`：播种 analyst 产出位；`plan_injected` 使 modeler 短路；plan_sha256 进 manifest
- 敏感性：`--plan` 路径缺 inject 为 `sensitivity_phase=stop`（旧路径仍 done 滑走）
- 交棒：`edit_code` 含 `reference add --force`；`edit_plan` 要求新开 run
- cumcm23-c `plan.json`：`plan check` 零缺口
- 定稿 00/10/13/13附/14/15 与根 README / ops_help 改为正式入口 `--plan`

## 自行拍板（非方向）

- 不做 plan.sensitivity_runs 短路（按拍板：子图仍 LLM 出计划）
- C4：冻结哈希失败时 WARN 提示 add --force，不另做 mtime 闸
- plan.sensitivity_runs 不写入图状态，避免与图内新跑混台账

## 未做

cumcm23-c 全图 LLM 真跑（D4）。

## D4 前置（inject 脚本）

- 新增 `problems/cumcm23-c/source/inject/sensitivity.py`：按研究网格重跑成本冲击 / 加成分位 / 损耗率缩放，打印 `RESULT: parameter=...`；中心点对齐 `_entry` 的 `q2_week_profit`（单测验证）。
- `wrap_inject_sensitivity`：`exec` 注入 `data_dir`，避开 `from __future__` 插队。
- `plan.json` 补 `sensitivity_relevant` 三条，把扫参参数名钉成 `成本冲击 (cost_shock)` 等，方便 LLM plan 与 inject 对上。
- 不做 `plan.sensitivity_runs` 短路。
