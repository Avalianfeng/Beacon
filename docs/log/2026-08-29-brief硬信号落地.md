<!-- doc: type=log status=active updated=2026-08-29 -->
# 2026-08-29 brief 硬信号落地（D-022 / C-01～C-04）

- 五阶段落地：有序块表 `NODE_BRIEF_SLICE` + `render_slice`；切片加宽（evaluation / paper_critic / modeler 红线 / coder `data_notes` / baseline）；schema v2 + S6 与 S9 同一 `load_brief`；S9 `hard_redline_violations` 恒 `stop`；S7 挂 `check_redlines` / `check_brief_claims`，报告记 `brief_sha256`；删除 `prompts/coder.py` 的 `build_prompt`（保留 SYSTEM）；C-04 `_jsonable` 列表不截断 + 门禁 retry 定向反馈。
- 数据源：`problems/mcm51-c/brief.json` 升 `schema_version: 2`，写入 7 条可机器化 `redline_rules`（hard 仅 `rl-result-format` / `result_rule`；其余 warn）。
- 验收证据：`uv run pytest tests/test_brief.py tests/test_insight.py tests/test_ops_review.py tests/test_routing.py tests/test_coder_baseline.py tests/test_ops_handoff.py tests/test_cli.py tests/test_restart.py` 相关用例通过（含 mcm51-c golden 现网 full/critic 逐字符、v2 `brief check`、hard 红线 stop、report 指纹）。`test_paper_expand.py::test_cli_expand_mock` 因网关 Connection error 失败，与本轮无关。
- 未做：ACT-08 注入 `background_knowledge`；ACT-01 拆 coder；ACT-02 全盘改重试；方案 B/C。
