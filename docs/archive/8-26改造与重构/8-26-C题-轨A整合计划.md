# 8-26 C 题轨 A 整合计划（已归档 · 压缩版）

> 状态：**已归档（2026-08-26 文档重构）**——计划已执行完毕（8-26-C题记录批次 8/9）。

## 原计划内容

目标：把 C 题已跑通的求解脚本登记为体系「参考实现」+ brief 补 solver 落地条目，使轨 A 流水线 coder 走「exec 参考实现 + 透传 RESULT」降级路径。验证范围（用户确认）：整合 + 本地验证 + `run --dry-run` 预检，不真跑轨 A。

## 执行结果

- 产物：`problems/mcm51-c/source/reference_solver.py`（2237 行）→ A1 拆分后为 `source/reference/` 7 文件；brief.json 51 条（formula-reference/formula-solver/red_lines 增补）
- 真跑结论（批次 9）：coder 环节 score 9/10 成功，下游 4 缺口 → **轨 A 出好论文降级为不重要（07 行 21）**
- 可复用机制与结论：`docs/18` §11.4
- 完整原文保留在 git 历史（可回溯）
