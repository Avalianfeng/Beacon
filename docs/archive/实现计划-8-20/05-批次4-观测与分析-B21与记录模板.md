# 批次 4：观测与分析 — 成本遥测 v1、评审相关性、记录模板

- 状态：**冻结（2026-08-21）**——v2 重心调整：观测分析不动「方向质量」主矛盾。
  仅「三、记录模板」继续复用：每次真跑前把三个模板复制进 run 目录、跑后填写（保证诚实口径）；
  成本遥测 v1 与 B21 评审相关性分析**不实施**（v0 脚本照常使用即可）。
- 目标：补齐 16 阶段 0 的「测量基线」——每次 run 后能回答「一分质量提升花了多少 token/时间/人工」，并让评审过程可审计。
- 依据：15 H04/H06、17 #1/#3/#7/#8/#9、B21、B22。

---

## 一、成本遥测 v1（17#1 完整化）

- v0（批次 0）已有：progress.jsonl 聚合脚本 `scripts/run_cost_summary.py`。
- v1 增强：
  1. **重试标注**：同节点多次 `node_start`（如 coder 内吞错重试、blueprint retry）→ 输出「重试次数与原因」（原因从 insights/*.md 或 state 摘要人工核对，脚本先标次数）；
  2. **模型价格**：`.env` 可选配置模型单价（token 单价表），脚本输出估算成本（无配置时只出 token 数，不硬编码价格）；
  3. **人工时间**：读取 run 目录约定的人工投入记录（见下 4.3），并入成本表「人工分钟」列；
  4. **废弃调用**：重试后未进入产物的 llm_call（按节点重入推断）单列。
- 约定：每次真跑后运行一次，产出 `runs/<run>/cost_summary.md`（进 run 产物目录，可审计）。

## 二、B21：评审相关性分析（17#9）

- 做法：
  1. 盘点各评审节点（analyst blueprint_critic / model_critic / model_code_consistency / paper_critic / figure_critic / evaluation）使用的**模型与提示文件**（读 `src/math_agent/config.py` MODEL_ROUTING 与 `prompts/*`、`src/math_agent/nodes/*_critic.py`）→ 输出矩阵表：节点 | 模型 | 提示文件 | 输入是否同源；
  2. 结论文档：标出「共享同一模型 = 相关偏差风险」的评审对（如 model_critic 与 model_code_consistency 若同模型，则「互评」独立性弱——对应 15 H04）；
  3. 建议（低侵入）：`run_manifest.json` 记录各评审节点实际模型版本（下次 schema 变更时顺手加，或本批次直接加）。
- 产出：`docs/实现计划-8-20/评审相关性盘点.md`（或并入 05 批次结果汇总，先出矩阵）。
- 验收：矩阵完整；明确列出相关评审对与独立性风险结论。

## 三、记录模板（17#3 / #7 / #8，供批次 5 起每次 run 使用）

在 `docs/实现计划-8-20/模板/` 建三个模板（随 run 复制到 run 目录填写）：

1. **评审记录模板**（17#3）：评分人（外部/内部）、评分依据（官方标准/自定 rubric）、逐题得分、分歧记录（多评审不一致处）、乐观偏差自查（是否知道输入来源）。
2. **人工投入记录模板**（17#7）：brief 编写分钟数、中途检查点分钟数、末尾修稿分钟数、每项对应 run 阶段。
3. **人工修补清单模板**（17#8）：修补处（文件/段落）、修补内容、修补原因、**是否把修补后的产物算作自动成功**（显式勾选，防止自动化率口径污染）。

## 四、与批次 5 的衔接

- 批次 5 的 brief-v2 真跑必须：跑前复制三模板进 run 目录 → 跑后填 → 成本遥测 v1 出表 → 评审按模板记录。三项齐全才算「测量基线完备」的 run。
