# 批次 0：简单修复与成本遥测 v0

- 状态：**已完成（2026-08-20）**——9 项全部落地，逐项状态见各小节
- 目标：一次把「低风险、小改动、单测可验证」的项全部清掉，并让 brief-v1 的成本表首次产出。
- 验收总则：每项 = 改动 + 相关子集 pytest 全绿 +（涉编译/涉 run 的）产物验证；全部完成后跑一次 `pytest tests\test_brief.py tests\test_routing.py tests\test_cli.py tests\nodes\test_analyst.py tests\nodes\test_blueprint_critic.py -q`。

---

## 0.1 M1：LaTeX 转义保护 `[`（B18，机制修复清单第一位）

- 改动点：`src/math_agent/nodes/latex_transform.py:513` `_escape_text_chars_skip_math(content, chars="_%&#")` —— `[` 不在转义字符集；`:540` 调用处 `chars="_%#"`。
- 做法（二选一，先做 A）：
  - A：默认 chars 增加 `[`，或对符号表符号列（裸 `[sigma]` 场景）做数学化包裹 `$[...]$`；
  - B：`latex_transform` 检测符号表单元格首字符 `[` → 强制数学模式。
- 测试：新增单测用例（`[sigma]`、`[%]` 等裸 `[` 不再产生 "Missing number" 类输出；数学模式内 `[` 不误伤）。
- 产物验证：用 `runs/51mcm-a-brief-v1/paper.md` 走一遍 latex 链路，`xelatex` 两遍无 "Missing number, treated as zero"；`compile.log` 干净。**不重跑 run**，只重编译。
- 完成后：B18 标「已解决」。
- ✅ **已完成（2026-08-20）**：方案 A——`_md_table_to_latex` 行首单元格 `[` 加 `{[}` 守卫（`\[` 是 display math，不可用）；tests/nodes/test_latex_transform.py +3 用例；paper.md 全篇重转换 + 临时副本 xelatex 两遍零错误；B18 已解决（14 表已标）。

## 0.2 M6：回归方程数值要求（brief 前置层，当前题）

- 改动点：`problems/mcm51-a/brief.json` 的 data_notes 字段增条目：要求 1.2 稳定段**必须输出回归方程系数数值**（如 P = a·T + b 的 a、b 实际数值 + R²），并保持条目容量 ≤200 字。
- 验证：`math_agent.cli brief check --brief docs\problems\mcm51-a\brief.json` 输出 `[OK]`；coverage 39→40 条（文档 12 §7 摘要与 00 关键数字的「39/39」改为新总数，注明 brief-v2 起生效）。
- 注意：M6 是**当前题要求**，不是通用机制（16 阶段 0 已注明）；不进 schema。
- ✅ **已完成（2026-08-20）**：data_notes 新增 `1.2-reg-coef`（40 条，`brief check` [OK]）；coverage 39→40 已同步（12 §七摘要、00 关键数字）；scripts/sample_brief_direction.py 加第 6 项检查。

## 0.3 M5：单位/量纲口径提示（最小版）

- 改动点：coder 生成 prompt（`src/math_agent/prompts/coder_baseline.py` 同目录的 coder 主 prompt）或 `render_coder_brief` 附加一条量纲要求：「数值必须携带并匹配物理单位；应力/力矩/力不得混比；力用 kN 级时数值口径一致；正文与模型章口径一致」。
- 测试：prompt 渲染单测（有/无 brief 时块存在性，沿用 test_brief.py 模式）。
- 说明：M5 完整版（校验层量纲检查）并入 coder 改进，归 07 评估；本项只做 prompt 层提示。
- ✅ **已完成（2026-08-20，prompt 层）**：`render_coder_brief`/`render_modeler_brief` 新增量纲口径块（`_dimension_note_lines`）；tests/test_brief.py +2 用例；校验层量纲检查仍待评估（07 行 8）。

## 0.4 B04：`_warn_brief_problem_mismatch` 规范化匹配

- 改动点：`src/math_agent/cli.py:385` —— 匹配前规范化：小写、去标点/空白/连字符、双向包含（problem_id ⊆ title 或 title ⊆ problem_id）。
- 回归用例：`mcm51-a` vs `2026 51MCM Problem A` → **不警告**（真跑误报案例）；真不匹配（`mcm52-a` vs 标题）→ 仍警告。
- 测试：tests/test_cli.py 补 2 个用例。
- ✅ **已完成（2026-08-20）**：落地为数字段/字母段 token 集合规范化匹配（`mcm51-a`↔`2026 51MCM Problem A` 不再误报，`mcm52-a` 仍警告）；tests/test_cli.py 实际 +3 用例。

## 0.5 B03：死代码清理

- 改动点：`src/math_agent/cli.py:551 / 986 / 1044` 残留 `brief_sha256 = None` 初始化行（supervise/start 死计算已删，仅剩无害死名）→ 删除。
- 验证：`py_compile cli.py` + test_cli.py 全绿。
- ✅ **已完成（2026-08-20）**：supervise/start 残留 `brief_sha256 = None` 死行删除；run 命令 551/554/589 活代码保留；py_compile + test_cli 全绿。

## 0.6 B13：行尾核验

- 现状：8-19 文档重组时已归一化（上次脚本检测全绿）。本项=写一次性核验脚本（检测 `\r\r\n`/`\r\r\r\n`/裸 CR）扫 `docs/**/*.md`，全绿后 B13 标「已解决」。
- 脚本落点：`scripts/check_doc_newlines.py`（可复用）。
- ✅ **已完成（2026-08-20）**：脚本已写入并全绿；B13 已解决（14 表已标；tests/test_brief.py 的 `\r\r\n` 历史遗留已记录，未归一化）。

## 0.7 B12：环境脏清理（需用户确认后执行）

- `runs/.beacon-active.json` 若指向 pytest 临时目录（`GET /api/active-run` 403 成因）→ **删除该文件需用户确认**（工作区安全规则）。确认后删除，Web 不再 403。
- ✅ **已完成（2026-08-20）**：用户确认后文件已删除，Web 不再 403。

## 0.8 B09：brief_coverage 门禁 stop 路径集成测试

- 现状：routing 纯函数有单测，图级（analyst 缺回应 → retry → 再缺 → stop → checkpoint `next=()`）无测试。
- 做法：graph 级测试（mock analyst/blueprint_critic 连续缺回应，断言 stop 映射与 checkpoint 状态）；以及门禁 stop 后 run 目录产物断言。
- 测试：tests/test_routing.py 或新增 tests/test_graph_gate.py。
- ✅ **已完成（2026-08-20）**：新增 tests/test_graph_gate.py 3 例——stop 路径（checkpoint `next=()`=recover 空转）、精确预算（iteration==MAX_BLUEPRINT_ITERATIONS）、followed 放行。

## 0.9 成本遥测 v0（高价值，可直接对 brief-v1 出表）

- 数据源已核实：`runs/51mcm-a-brief-v1/progress.jsonl` 含 `llm_call`（model/prompt_tokens/completion_tokens/latency_ms/node，63 条）与 `node_start`/`node_end`（duration_ms，79 对）。
- 交付：`scripts/run_cost_summary.py <run_dir>` 输出 markdown 表：节点 | 调用数 | prompt tokens | completion tokens | 墙钟 | 重试次数（同节点多次 node_start）；汇总行。
- **立即产出 `runs/51mcm-a-brief-v1/cost_summary.md`**（17#1 的 brief-v1 成本数据由此完成）；总量与 run_manifest/日志对照。
- 验收：brief-v1 出表；数字与 progress.jsonl 一致（可抽查一条 llm_call 对账）。
- ✅ **已完成（2026-08-20）**：`scripts/run_cost_summary.py` 产出 `runs/51mcm-a-brief-v1/cost_summary.md`（63 调用 / 54.1 万 tokens / 27.5 min；glm-4v 视觉评审占 43%）；17#1 v0 部分完成。

---

## 依赖与风险

- 无外部依赖；0.7 需用户确认；0.1 若改符号表数学化（方案 B）风险略高，先方案 A。
- ✅ 全部完成（2026-08-20）：00 更新记录已补「批次 0 完成」行；后续按新战略 **00 onboarding 优先**，批次 1（schema 扩展设计）排其后。
