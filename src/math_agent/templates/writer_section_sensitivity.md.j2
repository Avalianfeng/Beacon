# 章节撰写：敏感性分析

本轮你**只**撰写一个字段：`sensitivity`。其余章节由其他轮次处理，不要输出。

{% include 'writer_iron_rules.md.j2' %}

---

## 本段大纲锚点
{{ outline_sensitivity }}

---

## 敏感性分析结果
{% if sensitivity_runs %}
{% for r in sensitivity_runs %}
- **参数 `{{ r.parameter }}`**：在取值 {{ r.values }} 下，指标 `{{ r.metric }}` = {{ r.results }}
  - 解读：{{ r.interpretation or "（未提供解读）" }}
{% endfor %}
{% else %}
（暂无敏感性结果——sensitivity 章节应说明「未执行敏感性分析」并给出定性讨论）
{% endif %}

## 代码运行结果（末尾摘要，供数字追溯）
{% for a in code_artifacts %}
### [{{ loop.index }}] {{ a.purpose }} — success={{ a.success }}
```
{{ a.stdout }}
```
{% endfor %}

---

{% if prior_critic %}
## 上一轮 PaperCritic 反馈（必须逐条回应）

PaperCritic 评分：{{ prior_critic.score }}/10，approved={{ prior_critic.approved }}。

**issues**（仅与本段相关，其余可忽略）：
{% for i in prior_critic.issues %}
- [{{ i.section }}] {{ i.problem }}
{% endfor %}

本次重写时**必须**逐条回应上述与本段相关的 issues；不能简单复述上一稿。
{% endif %}

---

## 字段字数预算与结构要求

| 字段 | 字数区间 | 强制结构 |
|---|---|---|
{% if sensitivity_runs %}
| `sensitivity` | 1000–1800 | 包含实验设计、中心点复现、参数敏感度排序、随机稳健性区别、误差边界与管理含义。无二维交互实验时直接说明，不用扩展设计凑篇幅。 |
{% else %}
| `sensitivity` | 400–800 | 必须写明未执行定量扫参；用上方代码 RESULT 做中心点复现，再按模型结构做定性排序与管理含义。禁止编造扰动幅度、敏感度系数或未出现在结果清单中的数字。 |
{% endif %}

**IRON RULE 1（禁编造）铁律**：sensitivity 中的所有数字必须可在上方「敏感性分析结果」或「代码运行结果」中追溯。

敏感性结论只能逐字引用上方列出的原始扫描结果；不得另写任何未出现在结果列表中的百分比。敏感性图会被排在对应参数讨论之后，正文使用“如图所示”就近引用并解读。

**主口径取舍（必须用这六个字）**：若本段有定量扫参，正文里必须出现原词「主口径取舍」，并写清扫参之后主方案是否改口、仍采用哪组基准参数。只写「线性变化」「高度敏感」不够——写后 `review-check --strict` 的 L4 闸按这句原词扫描。

{% if retrieved_context %}

---

## 检索到的参考资料（仅供启发，不可照抄）
{{ retrieved_context }}
{% endif %}

---

## 输出契约

请返回严格符合下列 schema 的 JSON（**不得包含其他键**）：

```json
{
  "sensitivity": str
}
```
