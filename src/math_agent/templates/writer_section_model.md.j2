# 章节撰写：模型的建立

本轮你**只**撰写一个字段：`model_section`。其余章节由其他轮次处理，不要输出。

{% include 'writer_iron_rules.md.j2' %}

---

## 本段大纲锚点
{{ outline_model_section }}

---

## 题目
{{ problem }}

## 已确认假设（{{ assumptions | length }} 条）
{% for a in assumptions %}
- {{ loop.index }}. {{ a.statement }}
{% endfor %}

## 最终模型与必要推导
{% for m in model_versions %}
{% if loop.last %}
{{ m.description }}

**方程**：
{% for eq in m.equations %}
- \[ {{ eq }} \]
{% endfor %}

**变量**：
{% for k, v in m.variables.items() %}
- `{{ k }}`：{{ v }}
{% endfor %}

{% if m.derivation_steps %}
**推导链**：
{% for d in m.derivation_steps %}
- **{{ d.title[:100] }}**
  - 动机：{{ d.motivation[:100] }}
  - 陈述：{{ d.statement[:100] }}
  - 结果：{{ d.result[:100] }}
{% endfor %}
{% endif %}

{% if m.notes %}**实现边界**：{{ m.notes }}{% endif %}
{% endif %}
{% endfor %}

---

{% if prior_critic %}
## 上一轮 PaperCritic 反馈（必须逐条回应）

PaperCritic 评分：{{ prior_critic.score }}/10，approved={{ prior_critic.approved }}。

**issues**（仅与本段相关，其余可忽略）：
{% for i in prior_critic.issues %}
- [{{ i.section }}] {{ i.problem }}
{% endfor %}

本次重写时**必须**逐条回应上述与本段相关的 issues；不能简单复述上一稿。
{% endif %}

---

## 正式主方案代码（模型叙述必须与此实现一致）
{% for a in code_artifacts %}
{% if a.evidence_role == "primary" %}
```python
{{ a.code }}
```
{% endif %}
{% endfor %}

## 字段字数预算与结构要求

| 字段 | 字数区间 | 强制结构 |
|---|---|---|
| `model_section` | 2500–4500 | 按赛题小问组织最终模型，不展示 basic→improved→final 的研发过程。每个小问包含建模动机、变量、目标/约束、求解接口和回答指标，用 `##` 标题分隔。 |

### 推荐结构（用 `##` 标题）

按 Blueprint 的实际小问组织，每个小问一节：
- 完整写出该问的目标函数与约束，不得只写局部子式
- 定义该问涉及的决策变量与辅助变量
- 区分硬约束、软惩罚与评价指标，说明求解实现中的可行性检查
- 只描述正式代码真实执行的算法与实验；禁止编造代码未实现或未经数值验证的方法（如 XGBoost、STGNN、遗传算法、ALNS、精确求解器等）
- 若实现是启发式可行解，不得宣称精确最优性

每段首句说明该模型如何回答对应小问。删去与赛题回答无关的工程审计、恢复流程和开发版本说明。

{% if retrieved_context %}

---

## 检索到的参考资料（仅供启发，不可照抄）
{{ retrieved_context }}
{% endif %}

---

## 输出契约

请返回严格符合下列 schema 的 JSON（**不得包含其他键**）：

```json
{
  "model_section": str
}
```
