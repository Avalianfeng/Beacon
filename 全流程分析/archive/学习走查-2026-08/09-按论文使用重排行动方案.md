<!-- 2026-09-02 · 按「建模论文使用」重排 · 不改项目 D-xx -->

# 按论文使用重排的行动方案

> 过滤原则：任何与「从题面到可交论文」无关的设计一律降级。机制债（D-022～D-027）已落地；当前主风险是**未用真跑验证**，不是再堆编排面。

## 0. 现在处在哪

- 操作面 S0–S8 已成形；论文本体由 Beacon 产（D-020）；coder 默认不 LLM（D-024），主路径是冻结资产 / inject。
- 范本：mcm51-c（全流程）、mathorcup16-c（烟测未打磨）、mcm51-b（赛后校准，假设 9 已坐实）。
- 路线图 R2 已完，R3 已开：**P-06 = `cumcm23-c` 探索性真跑**（2026-09-02）。交棒以 [新题真跑-流程卡.md](新题真跑-流程卡.md) 为准（对撞后再 brief；不搜题解）。
- D1 已拍：不先旧题冒烟，直接新题验证功能并允许一版结果实验。评分由人手动。

---

## 1. 降级（与出论文弱相关，默认不排期）

| 项 | 为什么降级 |
|---|---|
| T-20 控制面/编排面、T-18 任意节点 restart、supervise 主路径 | 编排能力，不增加论文证据 |
| T-02 回退 CLI、T-04/T-05/T-10 探查与对撞命令化 | 人/agent 写文件已能交棒（D-021） |
| 前端完整形态、40 万字截断、非 TTY watch、status 静默改指 | 观察壳，不改论文 |
| M2 图注入、M4 对照通用化、pilot CLI、RAG、轨 A 出好论文 | 已拍板不做/冻结 |
| E-02～E-06 主张-证据库 / 方法库 / 竞赛模拟 / 智慧中枢复评 | 战略远期 |
| E-08 模块 1/5 改法规划文档 | 无题压时的蓝图，不挡做题 |
| ACT-10 evidence-package 文件列表、ACT-13 正赛大量实验模式 | 未碰到痛点前不建 |
| ACT-08 `background_knowledge` 注入 | 控制面已明确不做 |
| A2 锚点闸门、成本遥测、跨题复用、#8/#9 视觉恒 ok | 挂账直至真跑打出缺口 |

「S0–S3 再走查一遍讲解」也不排期：全流程分析六站已完成；缺的是**真跑验证**，不是再讲一遍。

---

## 2. 核心环节：要不要验证 / 优化

按对终稿论文的影响排序。

| 环节 | 判断 | 要做的事 |
|---|---|---|
| **S9 / S6–S7 在 D-022 之后** | **必须验证**。切片改了 5 处 prompt，单测盖不住 LLM 行为；红线 warn 零观察样本 | 随 `cumcm23-c` S6–S7 观察，不单开旧题冒烟 |
| **S5 冻结资产执行（T-19）** | 已落地，是现行主求解路径 | 冒烟时走 T-19；顺带看「frozen 分支零 brief」是否伤论文口径 |
| **S7 评审闸门** | 数字红线/L4/brief claims 已有；**仍有硬缺口** | `check_redlines` 常缺 `--code/--stdout`；`brief_coverage` 只核 id；操作面 `check_l4_gates --brief` 是死参数 |
| **S6 论文化** | expand 已有；对照/敏感性/作图链仍弱 | `figure_plan` 不到作图链 → 图与方向易漂；无 inject 时敏感性 skip（论文 degraded 已接受） |
| **S0–S3 方向** | **质量杠杆最高，但不是缺命令**。赛后根因多是口径/假设（docs/20），不是缺回退 CLI | 不先做 extract/前端；用模板与人检收紧（波次 C，需拍板） |
| **S4 预检** | 够用（存在性 + schema） | 不优化 |
| **观察面 / Web** | 与论文无关 | 不排期 |

---

## 3. 还没决策好的（短清单）

正式登记里待决几乎清空。真正未拍板、且影响下一步的只有这些：

| # | 事项 | 不决策的代价 | 建议 |
|---|---|---|---|
| **D1** | 旧题冒烟 vs P-06 开新题 | 新题会把未验证切片带进真跑 | **已决（2026-09-02）= 开 `cumcm23-c`**；接受探索性、允许中途停 |
| **D2** | T-03 `brief extract` / ACT-06 S0 填空模板 / 都不做 | 方向仍全靠人+强模型手工；与出论文相关，但是否值得建机制未决 | 建议：**先不做代码**；做题时用 playbook 当清单。要机械化再单独立项 |
| **D3** | 是否修 S7 红线缺 code/stdout、coverage 仅 id | 闸门对「代码是否踩红线」仍偏盲 | 冒烟后若打出漏报再修；若一次都打不中可继续挂 |
| **D4** | frozen 路径要不要吃 brief 切片 | T-19 主路径可能完全不看方向字段 | 冒烟时记一笔；伤口径再开任务 |
| **D5** | 07 行 19 双入口实验（extract vs 外部 brief） | 与 D2 绑定；方向已定未实现 | 并入 D2，不单开实验 |
| P-06 | 新题开题 | 烧 token；本身即盲测样本（D-016） | **进行中**：cumcm23-c |

**不是待决、勿再讨论**：ACT-08、M2/M3/M4、T-20、轨 A 出好论文、正赛大量实验（ACT-13 保持「待细化、无题不建」）。

文档漂移（不是真待决）：07 行 20 仍标 `proposed`，D-A～D-E 实际已闭环——下次改 07 时改状态即可，不阻塞工作。

---

## 4. 短板（相对「交得出、对得上证据的论文」）

1. **D-022～026 无真实 run 证据**（最大验证债）。
2. **方向质量仍在体系外**：错误假设能一路走到论文（mcm51-b 假设 9）；闸门核数字不核「假设是否题面允许」。
3. **操作面几乎不读 brief 业务字段**：S6 只吃 2 个字段；S7 评审与 brief 长期脱钩，C-02 补了 claims 脚本但真跑未证。
4. **作图与对照**：figure_plan 不达作图；M4 冻结导致对照靠手工——赛后论文常见弱项。
5. **T-16 gap 检查绑调度键名**：非调度题机械漏检。
6. **无完整「新题→终稿论文」的第二份打磨样本**：mathorcup 只证明走通，不证明论文可用。

---

## 5. 具体行动（按波次，可直接开干）

### 波次 A — 进行中（`cumcm23-c` 探索性，不单开旧题冒烟）

S0–S8 按 [流程卡](新题真跑-流程卡.md)。允许中途停。S6–S7 顺带观察 D-022 切片。评分人手动。

不在本波次：extract、前端、ACT-08、S9 主路径。

### 波次 B — 真跑打出洞再修

只修审查表里「挡论文」的项。

### 波次 C — 做题时收紧方向（低代码）

对撞前对齐图；探索按八字段；brief 在对撞后。

### 波次 D — 明确不做（直到缺口打出来）

见 §1。

---

## 6. 现行一句

**正在跑 `cumcm23-c`；对撞后才 brief；探索性，不要求一次走完。**
