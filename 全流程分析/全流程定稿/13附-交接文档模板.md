<!-- 模板 · type=handoff · 研究收束 → Beacon 操作的题级交接文档 -->

# <题ID> · 交接：研究 → Beacon 操作

> **用途**：研究者收束后、登记代码前，由主会话（或研究者本人）按本模板在题根写 `problems/<题>/交接-beacon.md`。
> **读者**：继续操作 Beacon 的会话——可以是原研究会话续跑，也可以新开会话读盘；本文让两条路等价，不依赖聊天记忆。
> **出处**：`全流程分析/全流程定稿/13附-交接文档模板.md`。
> **实践范例**：`docs/log/2026-09-04-cumcm23-c-交接-Beacon后续.md`（S5 链由续会话凭开工白跑通）。

---

## 0. 一句话状态

<!-- 研究是否收束；brief check 是否过；预检状态；证据链通到哪；当前卡点一句话 -->

## 1. 磁盘真相（勿靠聊天记忆）

| 路径 | 含义 |
|---|---|
| `<题根>/_研究日志.md` 等研究外显 | 过程记忆 / 解题说明 / 经验与坑 / exploration（人读收束） |
| `<题根>/研究/{scripts,data,figures}/` | 实算资产；定方向与 `_entry`/inject 网格；**不是**论文发表数字 |
| `<题根>/source/reference/_entry.py` | 冻结主求解：Q 行 + RESULT；须能 open 声明附件并落盘 PNG |
| `<题根>/brief.json` | 方向手柄（schema v2，`brief check` 状态） |
| `<题根>/plan.json` | 机读蓝图+模型卡（`plan check`；coverage 须带小问/方程锚） |
| `runs/<题>-reference/evidence.json` | 图外冒烟；给人闸与 `--strict` 对账，**不是**论文数字源 |
| `<题根>/source/inject/sensitivity.py` | 图内敏感性；研究加成带/成本冲击表只作参数参考，数字以图内重跑为准 |
| `runs/<题>-preflight/preflight.json` | 预检结果（ok / blockers） |
| `<题根>/认知地图集/` | 地图 / Diff / 清点（方向资产，不在题根） |

## 2. 关键数（与 CSV/JSON 对齐）

<!-- 只放人抽查过、能复算对齐的数字；每行标注出处（研究/data 哪张表、哪个脚本、哪个字段） -->

| 关键数 | 值 | 出处 |
|---|---|---|
| … | … | `研究/data/….csv` 行/列 |

## 3. 人闸记录

| 时间 | 谁 | 确认了什么（拍板微调 / 抽查项） | 记录处 |
|---|---|---|---|
| … | 人 / 主会话 | … | `_研究日志.md` 对应条目 |

> 人闸没真做就写「未过」，不许替写通过；`recertify` 的 actor/notes 与本表对齐。

## 4. 定稿站位

<!-- 用 `math-agent problem stage <题> --json` 对齐 runs/ 事实，不凭记忆 -->

| 定稿站 | 状态 | 下一步 |
|---|---|---|
| 10 研究 | ☑ 收束 / ☐ | — |
| 11 brief | ☑ check OK / ☐ | — |
| 12 预检 | ☑ / ☐ | — |
| 13 证据登记 | ☐ 本站 | plan check + add/run/verify；inject/sensitivity.py |
| 14 启动图 | ☐ 待命 | `run --plan`（不要首枪 `--from writer`） |

## 5. 命令序（登记 → 启动图）

<!-- 按本题实写；每步给出期望产物。改代码后 = 新一轮登记，不是一次性交差 -->

```text
# ① plan 预检 + 登记冒烟（可反复：改代码 → add --force）
uv run math-agent plan check --plan problems/<题>/plan.json --brief problems/<题>/brief.json
uv run math-agent reference add      --problem problems/<题>/problem.json --solver problems/<题>/source/reference --entry _entry.py --force
uv run math-agent reference run      --problem problems/<题>/problem.json --out runs/<题>-reference
uv run math-agent reference verify   --problem problems/<题>/problem.json --evidence runs/<题>-reference/evidence.json
uv run math-agent reference recertify --problem problems/<题>/problem.json --evidence runs/<题>-reference/evidence.json --actor "<会话标识>" --notes "人闸见第 3 节；敏感数字将由图内 inject 重跑" --verdict pass

# ② 正式启动全图（论文数字 = 图内 execute stdout）
uv run math-agent run --problem problems/<题>/problem.json --brief problems/<题>/brief.json --plan problems/<题>/plan.json --out runs/<题>-graph
```

敏感数字：研究加成带/成本冲击表是参数参考；论文以 `source/inject/sensitivity.py` 图内重跑为准。

期望产物：`plan.json` check 过；冒烟 evidence；② 后 `runs/<题>-graph/`。

## 6. 已知缺口（告知即可）

<!-- 非硬挡项与失效对照，防止下一会话踩：stage 误报、WARN、已否证的旧对照、缺文件非硬挡 … -->

## 7. 禁止

<!-- 按题裁剪，至少保留：联网搜本题题解当依据；手改 研究/data 绑定数字装进论文；未授权代跑论文链 / 代 accept -->

## 8. 可粘贴开工白（给 Beacon 操作会话）

```text
你接手 <题> 的 Beacon 后续。人闸状态见交接文档第 3 节。读：
  <题根>/交接-beacon.md → brief.json → _研究日志.md
当前：<第 0 节一句话>。请：按第 5 节命令序执行；预期停点与处理见定稿 00 第 5/8 节与 14/15。
不要默认开 <未授权段>。工作区 E:\git_clone\Beacon。
```

## 9. 执行回写（Beacon 操作会话在此追加，一行状态 + 时间）

<!-- 例：2026-09-04 22:06 | 续会话 | S5 链已通（run+verify+recertify pass），数字与第 2 节对齐。下一步问人是否开 14。 -->
