# 说明 · 怎么写读图任务（新题临时生成）

每张对撞用图写一行：

| 图 | 画了什么 | 对撞时请回答 |

另附「图说了什么」：无图像模型的底线描述（有图则以图为准）。

原则：图是共享视觉证据，不是论文图；对撞时每张至少用一次，支持淘汰方法或提出拍板点，不要只重复文字统计。

拼图命令示例：

```text
python scripts/pack_external_upload.py --dir problems/<题>/eda --glob-png "附件*.png" --stem 概览四合一 --md-skip
```
